# Registrul redesignului: tot ce se schimba, si ce cere fiecare lucru la final

**Regula proprietarului, 20.09.2026:** *„eu as vrea la final sa aplicam tot in productie, nu
acum, dar sa tii cont si sa stii mereu ce adaugam/modificam nou ca la final cand dam push sa
faci totul perfect."*

Deci: pana la sfarsitul redesignului, **productia nu se atinge cu nimic** (nici baza, nici
Vercel, nici push). Tot ce s-ar fi aplicat acum se scrie aici si se aplica atunci, in ordinea
de mai jos.

⚠ Fisierul asta se scrie **in aceeasi clipa** cu schimbarea, nu la final din memorie. O
schimbare care nu e aici e o schimbare pe care nimeni n-o mai gaseste in ziua unirii.

Dosarul demo si curatenia de dupa: [`demo-seed/LA-FINAL.md`](../../../demo-seed/LA-FINAL.md).

---

## A. Ce trebuie facut in ziua unirii, in ordine

**Ordinea nu e cosmetica.** Codul de pe ramura cheama functii care in productie nu exista
inca. Pus invers, panoul principal ar arata „Graficul de vanzari nu a putut fi incarcat" intre
push si aplicarea migratiei, la toti comerciantii.

1. **Intai migratiile** din tabelul B, in productie (`rtefdpioqmowkdiybwrr`), in ordinea din
   tabel.
   ⚠⚠ **NU MAI E NICIUNA DE APLICAT. TOATE SAISPREZECE SUNT IN PRODUCTIE.** 1-10 de la
   unirea din 20.09; 11 separat pe 21.09; 3b era deja acolo, desi tabelul spunea altceva;
   12-16 pe 21.09, dupa ce s-a vazut ca fila era RUPTA in productie (nota de sub tabel).
   Verificat obiect cu obiect, nu presupus, si schema de referinta regenerata dupa.
   ⚠ **Pasul asta a ramas aici ca sa se stie ca NU mai e nimic de facut la el.** Reaplicate,
   migratiile dau `42P07` in mijlocul sirului, iar migratia 5 ar sterge tabele care au azi
   date adunate zi de zi. Deci in ziua unirii: **nu se atinge niciuna**, se trece la pasul 3.
2. **Verificarea drepturilor** dupa fiecare migratie: `anon` NU trebuie sa poata chema
   functiile noi (vezi nota din C).
3. **Regenerarea schemei de referinta**: `bash scripts/schema-baseline.sh`, apoi
   `bash scripts/schema-baseline.sh --check` pana scrie „schema din Git = schema din productie".
4. **Variabilele noi de mediu** din tabelul D, in Vercel (Production), inainte de push.
5. **Verificarile** pe ramura: `npx tsc --noEmit`, `npm test`, `npm run build`, `eslint`.
6. **Unirea si push-ul**, apoi verificat: productia raspunde, CI verde, Sentry fara erori noi.
7. **Curatenia** din `demo-seed/LA-FINAL.md` (baza demo, cele 8 chei de ramura, dupa id).

⚠ **Dupa push, de verificat pe productie ce nu se poate verifica pe demo**: cosurile
abandonate au acolo TRAFIC REAL (393 de cosuri, 34 de emailuri si 21 de SMS-uri deja plecate
catre clienti). Se urmareste ca `recovery_sends` chiar primeste randuri la prima trimitere de
mana, si ca `deschis_la` se scrie cand cineva apasa linkul. Pana atunci, atribuirea e cod
probat, nu cod vazut lucrand pe oameni.

---

## B. Migratii de baza de date

> ⚠⚠ **CORECTAT PE 21.09.2026, DUPA O VERIFICARE PE PRODUCTIE.** Tabelul spunea
> despre opt migratii (2, 4-10) ca sunt „de aplicat la final". Toate erau DEJA
> aplicate, de la unirea din 20.09 - se vede si din commitul „Schema de
> referinta, regenerata dupa aplicarea migratiilor in productie".
>
> Nu era o greseala de scris, ci o capcana: in ziua unirii, cine urma lista ar
> fi incercat sa le aplice a doua oara si ar fi primit `42P07: relation already
> exists` la mijlocul unui sir de migratii - adica exact in clipa in care nu stii
> daca ai stricat ceva. Iar migratia 5 ar fi STERS si recreat tabele care au azi
> date adunate zi de zi.
>
> Starea de mai jos e citita din productie, obiect cu obiect, nu presupusa.


| # | Fisier | Ce aduce | Aplicata in DEMO | Aplicata in PRODUCTIE |
|---|--------|----------|------------------|------------------------|
| 1 | `migrations/2026-09-20-produse-sub-prag.sql` | `stoc_combinatie`, `combinatie_aprinsa`, `produse_sub_prag`, `numar_produse_sub_prag` (stoc scazut vazut si pe variante) | DA | **DA, 20.09.2026** (aplicata inainte de regula de mai sus; schema de referinta a fost regenerata atunci) |
| 2 | `migrations/2026-09-20-vanzari-panou.sql` | `fereastra_vanzari`, `canale_vanzare`, `vanzari_panou` (graficul de vanzari: perioade, canale, comparatie) | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. |
| 3a | `migrations/2026-09-20-panou-carduri.sql`, partea de jos | politica RLS lipsa de pe `business_daily_stats` | DA | **DA, 20.09.2026**, cu acordul lui: repara un defect care lovea cei 71 de comercianti cu statistici. ⚠ La final NU se mai aplica a doua oara (ar da `42710: policy already exists`): se sare peste ultima parte a fisierului. |
| 3b | `migrations/2026-09-20-panou-carduri.sql`, functia | `panou_carduri` (cele patru carduri din cap) | DA | **DA.** ⚠ Registrul spunea „de aplicat la final”; verificat pe 21.09.2026 direct in productie, functia EXISTA. A plecat odata cu unirea din 20.09. |
| 4 | `migrations/2026-09-20-analitice-sesiuni.sql` | sesiuni si vizitatori in `site_analytics` (coloane + indexuri), tabela `analitice_sare` si functia `analitice_sarea_zilei` | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. ⚠ Prima migratie care schimba o TABELA, nu doar adauga functii: patru coloane noi, toate optionale. |
| 5 | `migrations/2026-09-20-analitice-agregat-sesiuni.sql` | tabelele `analitice_zilnic` si `analitice_zilnic_sursa` (+ politici) si `agregeaza_analitice` care le umple | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. ⚠ Dupa aplicare, primele zile de sesiuni se strang la urmatoarea rulare a cronului `discount-release`; istoricul NU se poate reconstrui, fiindca randurile brute mai vechi de 8 zile nu mai exista. |
| 6 | `migrations/2026-09-20-trafic-si-harta.sql` | `trafic_panou`, `trafic_pe_sursa`, `comenzi_pe_judet` | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. |
| 7 | `migrations/2026-09-20-fereastra-azi-ieri.sql` | `fereastra_vanzari` capata „azi" si „ieri" | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. ⚠ Inlocuieste functia din migratia 2, deci se aplica DUPA ea. |
| 8 | `migrations/2026-09-20-palnie-si-venit-pe-sursa.sql` | `site_analytics.valoare`; `analitice_zilnic.sesiuni_cu_produs/_cu_cos/_cu_checkout`; `analitice_zilnic_sursa.vanzari`; `agregeaza_analitice` rescrisa ca sa le umple; `palnia_panou`; `trafic_pe_sursa` refacuta cu venit | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. ⚠ Atinge o TABELA cu trafic real (`site_analytics`) si cele doua tabele de agregat; toate coloanele sunt optionale sau cu implicit. Se aplica DUPA migratiile 4 si 5. ⚠ `trafic_pe_sursa` se sterge si se recreeaza (semnatura de intoarcere se schimba), deci ordinea fata de migratia 6 conteaza. |
| 9 | `migrations/2026-09-20-vanzari-detaliu.sql` | `vanzari_detaliu` (sumarul, produsele, categoriile, canalele si starile filei Vanzari) si `carduri_secundare` (clienti noi, clienti care revin, bucati, anulari - cu fereastra precedenta) | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. Numai functii noi; nu atinge nicio tabela. Se aplica DUPA migratia 2 (foloseste `fereastra_vanzari`). |
| 10 | `migrations/2026-09-20-verde-rebranding.sql` | Implicitul lui `businesses.primary_color` trece de la `#1AB554` la `#07c527` | DA | **DA, 20.09.2026** (la unirea de atunci). ⚠ NU se mai aplica o data. Doar `set default`, deci atinge numai magazinele FACUTE DE ACUM INAINTE. ⚠ Cele existente NU se ating, si e o hotarare: `primary_color` e culoarea comerciantului, nu a noastra, iar un `update` peste randurile ramase pe vechiul implicit ar repicta intr-o noapte vitrine care nu ne-au cerut nimic. |

| 11 | `migrations/2026-09-21-suprimare-contacte.sql` | `recovery_optout` capata `phone` si `motiv`; `email` devine optional; o restrictie care cere macar un contact; index unic pe (magazin, telefon) | DA | **DA, 21.09.2026**, cu acordul lui. Verificat pe amandoua bazele: coloanele exista si `email` e `nullable`. |

| 12 | `migrations/2026-09-21-mesaje-recuperare.sql` | Tabela `recovery_sends` (jurnalul mesajelor de recuperare) cu index unic pe `(cos, canal, cheie)`, doua indexuri si politica de citire pentru comerciant | DA | **DA, 21.09.2026**, cu acordul lui, fiindca ecranul cerea obiectul asta si era RUPT in productie (vezi nota de sub tabel). ⚠ La aplicare s-a gasit ca Supabase daduse lui `anon` SELECT la nivel de TABELA, spre deosebire de toate tabelele surori. Revocat pe amandoua bazele, si adaugat in fisierul de migratie. |

| 13 | `migrations/2026-09-21-cosuri-ignorate.sql` | `abandoned_carts.ignorat_la` (un cos ramane in cifre, dar nu mai primeste mesaje) si index partial pe cosurile care pot fi contactate | DA | **DA, 21.09.2026**, cu acordul lui, fiindca ecranul cerea obiectul asta si era RUPT in productie (vezi nota de sub tabel). ⚠ Coloana ASTA rupea si cronul: el filtreaza pe ea, iar o coloana lipsa rupe toata interogarea. |

| 14 | `migrations/2026-09-21-atribuire-recuperare.sql` | `recovery_sends.deschis_la` si `.comanda_id` (+ index partial): linkul de recuperare lasa urma, si comanda se leaga de mesajul care a adus-o | DA | **DA, 21.09.2026**, cu acordul lui, fiindca ecranul cerea obiectul asta si era RUPT in productie (vezi nota de sub tabel). Aplicata DUPA 12. |

| 15 | `migrations/2026-09-21-cosuri-sumar.sql` | `cosuri_abandonate_sumar`: toate cifrele filei, socotite in baza pe o fereastra data (inclusiv cele trei feluri de recuperare) | DA | **DA, 21.09.2026**, cu acordul lui, fiindca ecranul cerea obiectul asta si era RUPT in productie (vezi nota de sub tabel). Aplicata DUPA 12 si 14. ⚠ Verificat in productie ca `anon` NU are EXECUTE si `authenticated` are. |

| 16 | `migrations/2026-09-21-cosuri-grafic-si-produse.sql` | `cosuri_abandonate_grafic`, `cosuri_abandonate_palnie` si `cosuri_abandonate_produse` (graficul, palnia si tabelul de produse al filei Prezentare) | DA | **DA, 21.09.2026**, cu acordul lui, fiindca ecranul cerea obiectul asta si era RUPT in productie (vezi nota de sub tabel). Aplicata DUPA 12 si 14. ⚠ Verificat in productie ca `anon` NU are EXECUTE pe niciuna. ⚠ Probate pe date adevarate: palnia da 321 → 51 → 41 → 0 → 0 si NU creste nicaieri. |

| 17 | `migrations/2026-09-21-clienti-adevarul-cifrelor.sql` | `comanda_incasata` (regula „au intrat banii", in SQL); `customers_aggregate` si `customers_summary` rescrise: `paid_order_count`/`total_spent` devin `valid_order_count`/`orders_value`, si apare `collected_total` | DA | **NU.** De aplicat la unire, INAINTEA codului. ⚠⚠ Numele vechi PROMITEAU bani si numarau altceva: excludeau doar anulatele si rambursatele, deci includeau comenzi neplatite. ⚠ Masurat pe productie: 88 de comenzi sunt `delivered` + `unpaid` + ramburs, adica **7.693,43 lei** care SUNT incasati desi plata scrie „neplatit" - de-aia rambursul livrat se socoteste incasat. ⚠ Regula e scrisa si in TypeScript (`lib/customers/bani.ts`); `regula-e-una-singura.test.ts` le tine lipite. |

| 18 | `migrations/2026-09-21-clienti-pe-perioada.sql` | `customers_summary` capata fereastra de timp (`p_de_la`, `p_pana`) | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 17. ⚠ Argumentele au implicit, deci un apelant vechi primeste exact ce primea. ⚠ Cardul „Clienti" ramane numarul TOTAL, netaiat pe perioada - hotararea lui, intrebat anume. |

| 19 | `migrations/2026-09-21-clienti-filtre.sql` | `customers_aggregate` capata `p_segment`, `p_valoare_min/max` si pragurile de VIP ca ARGUMENTE | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 17. ⚠⚠ Pragul de VIP nu e scris in functie: masurat, cel mai mare client al platformei are 968,99 lei in tot istoricul, deci un prag fix de 1.000 n-ar fi aprins segmentul pentru nimeni, in niciun magazin. ⚠ Cautarea a intrat in PARANTEZE: fara ele, `or`-urile ei s-ar fi lipit de filtre si orice rand potrivit la nume ar fi trecut de toate. |

| 20 | `migrations/2026-09-21-clienti-activitate.sql` | `customer_activity`: cronologia unui client din patru izvoare (comenzi, cosuri, SMS, mesaje de recuperare) | DA | **DA, 21.09.2026**, cu acordul lui. Numai o functie noua. ⚠⚠ Fiecare izvor e adus la ACEEASI cheie in cascada (telefon normalizat → `email:<adresa>`); una compusa altfel intr-un singur izvor ar fi dat o cronologie care SARE peste jumatate din ce s-a intamplat, fara nicio eroare. ⚠ Randurile fara niciun contact se sar: `'' = ''` e adevarat, deci s-ar fi lipit de orice client cu cheia goala. |

| 21 | `migrations/2026-09-21-clienti-segmente-si-importuri.sql` | Tabelele `customer_segments` (criterii `jsonb`, index unic pe nume normalizat) si `customer_imports` (istoricul importurilor), amandoua cu RLS, `revoke select from anon` si indexuri pe cheile straine | DA | **DA, 21.09.2026**, cu acordul lui. ⚠ `revoke select from anon` e pus dinadins: Supabase da implicit toate drepturile pe o tabela noua din `public` - lectia de la `recovery_sends`, din chiar ziua asta. ⚠⚠ Indexul unic normalizeaza numele el insusi (`lower(btrim(regexp_replace(...)))`); prima scriere se bizuia pe curatarea din TypeScript, si probat pe demo „  vip DE valoare Medie  " intra linistit pe langa „VIP de valoare medie". |

| 22 | `migrations/2026-09-21-clienti-regula-segmentului-una-singura.sql` | `customers_merged` si `customer_in_segment` (scoase din `customers_aggregate`), `customers_aggregate` rescrisa peste ele, si `customer_segment_counts` (cati sunt in fiecare segment) | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 19 si 21. ⚠⚠ Fila „Segmente" spune cati sunt, iar lista arata care sunt: doua copii ale aceleiasi reguli s-ar fi despartit, si placa ar fi spus 98 acolo unde lista arata 140, fara nicio eroare. ⚠ **Nimic din ce se vede nu se schimba**: aceleasi zece cifre pe segment inainte si dupa mutare, verificate pe demo pe toate trei drumurile. ⚠ `customers_merged` e `language sql` + `stable` ca sa fie INCLUSA in planul celui care o cheama; scrisa in plpgsql, filtrele n-ar mai fi coborat inauntru. |

| 23 | `migrations/2026-09-21-clienti-adaugare-si-stergere.sql` | `customer_add_manual` (adaugarea unui client de mana) si `customer_delete_contact` (stergerea unui contact FARA comenzi) | DA | **DA, 21.09.2026**, cu acordul lui. Numai functii noi; nu atinge nicio tabela si nicio coloana. ⚠⚠ Paza stergerii e in CHIAR `delete ... where not exists`, nu intr-o citire de dinainte: intre cele doua incape comanda omului, plasata in secunda aceea. Probat pe demo: stergerea unui cumparator da 0 randuri, `customers` ramane la acelasi numar. ⚠ Adaugarea intoarce o STARE, nu doar randul: „exista" si „are-comenzi" sunt lucruri deosebite si au mesaje deosebite. |

| 24 | `migrations/2026-09-21-clienti-de-unde-vine-contactul.sql` | `customers_merged` si `customers_aggregate` duc mai departe `customers.source` | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 22 si 23. ⚠⚠ REPARA O ETICHETA CARE MINTEA: badge-ul „Importat" se punea dupa `orderCount = 0`, adevarat exact cat timp importul era singurul drum. In ziua in care s-a scris adaugarea de mana, un om luat la telefon a aparut scris „Importat". ⚠ Amandoua functiile se STERG si se recreeaza (lista de coloane intoarse se schimba), in ordinea: intai lista, apoi `customers_merged`. Semnatura ARGUMENTELOR nu se schimba, deci niciun apelant nu se atinge. ⚠ Verificat pe demo: aceleasi zece cifre pe segment, pe amandoua drumurile. |

| 25 | `migrations/2026-09-21-clienti-judet-si-canal.sql` | `customers_merged` capata `canal`; `customers_aggregate` capata `p_judet` si `p_canal`; functia noua `customer_filter_options` (ce judete si ce canale EXISTA in magazin, cu cate randuri fiecare) | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 22, 23 si 24. ⚠ Amandoua functiile se sterg si se recreeaza (coloane si argumente noi), intai lista, apoi `customers_merged`. ⚠⚠ Meniurile se fac DIN DATE: cele 42 de judete ale tarii la un magazin care livreaza in douasprezece ar fi insemnat treizeci de optiuni care nu gasesc pe nimeni. ⚠⚠ Canalul se ia din cheia `marketplace` a lui `order_source`, NU din campul intreg: acela are 465 de valori deosebite din 537 de comenzi. ⚠ Probat pe demo: `judet=Cluj` + `canal=magazin` da 15 clienti si pe ecran, si in baza. |

| 26 | `migrations/2026-09-21-clienti-anonimizare.sql` | `customer_anonymize`: sterge datele personale ale unui client din comenzi, cosuri, retururi si jurnalul de SMS, si sterge randul lui din `customers` | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 25. ⚠⚠ NU atinge `recovery_optout` si `sms_optout`: omul care cere sa fie sters e de multe ori chiar cel care ceruse sa nu mai primeasca mesaje, iar sters si randul acela ar reintra pe liste a doua zi. ⚠⚠ NU sterge comenzi: venitul lunilor incheiate ar scadea retroactiv, iar facturile ar ramane fara nimic in spate. ⚠ Masurat pe demo cu rollback, pe un om cu 3 comenzi: venit 118.875,72 → 118.875,72, 393 comenzi → 393, 358 clienti → 358 (UN singur rand anonim, nu trei), zero date personale ramase. ⚠ `orders.customer_phone` e `not null`: se scrie sirul gol, nu `null` — baza a oprit prima incercare. |

| 27 | `migrations/2026-09-21-clienti-segmente-cu-lista.sql` | `customer_segments.fel` ('criterii' / 'lista'), tabela `customer_segment_members`, si `p_chei` pe `customers_aggregate` | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 26. ⚠ Al doilea fel de segment, cerut anume ca actiune in masa, desi i-am spus ca nu se potriveste cu felul in care sunt facute segmentele: unul cu lista NU se mai schimba singur. Deosebirea e scrisa pe ecran de doua ori. ⚠⚠ `p_chei = null` inseamna „fara filtru pe chei", NU „nicio cheie": scris cu `coalesce(p_chei,'{}')`, pagina de clienti s-ar fi golit pentru toata lumea. Probat: fara filtru 358, cu doua chei 2, cu lista goala 0. ⚠ Politica RLS trece prin segment si apoi prin `businesses`; scrisa doar pe `segment_id`, oricine ar fi citit lista altcuiva ghicind un uuid. |

| 28 | `migrations/2026-09-21-clienti-audit.sql` | `customer_segment_sizes` (numara membrii unei liste in baza); `comanda_incasata` capata `security invoker` + `search_path` fixat; `revoke insert/update/delete` de la `anon` pe cele trei tabele noi | DA | **DA, 21.09.2026**, cu acordul lui. ⚠⚠ Repara un defect scris tot azi: fila „Segmente" aducea CHEILE membrilor (`limit(10000)`) si le numara in JavaScript — plafoanele ingaduie 50 x 500 = 25.000 de randuri, deci taia TACUT, iar peste asta e si plafonul PostgREST (lovit deja o data la 1.000). ⚠ Corpul lui `comanda_incasata` ramane LITERA CU LITERA acelasi: o prima scriere „curata" pe drum si comparatia metodei de plata cu `lower()`, ceea ce ar fi MUTAT BANII dintr-o migratie care spune ca doar fixeaza `search_path`. Masurat: zero comenzi au metoda scrisa cu majuscule. |

| 29 | `migrations/2026-09-21-clienti-vip-doar-pe-valoare.sql` | `customer_in_segment` judeca VIP numai pe valoare, de la 10.000 lei; `p_vip_comenzi` iese din semnatura lui, a listei si a numaratorii | DA | **DA, 21.09.2026**, cu acordul lui. Aplicata DUPA 28. ⚠⚠ Cerut de el: „doar daca a comandat de peste 10.000 lei" — deci numarul de comenzi IESE din regula, nu i se urca pragul. ⚠⚠ Masurat INAINTE: productia are 494 de clienti si cel mai mare a cumparat vreodata de 699 lei; peste 10.000 sunt ZERO, si pe demo, si pe productie. Segmentul va da zero pana cand cineva cumpara de zece mii — hotararea lui, luata cu cifra la vedere. ⚠ Probat pe demo: VIP a trecut de la 25 la 0, celelalte noua segmente neschimbate, iar lista si placa spun acelasi lucru pe toate zece. |
| 30 | `migrations/2026-09-21-discounturi-cifrele.sql` | `discount_stats(bid)` (cate comenzi, cati bani dati, cate vanzari, cate cazute, cate cu transport oferit) si `discount_orders_fara_legatura(bid)` | NU | Nu inca. ⚠ `uses_count` SCADE la anulare, deci nu raspunde la „de cate ori s-a folosit": masurat pe demo, `BINEAIVENIT10` scria 26 pe un cod folosit pe 30 de comenzi. De-aia se arata amandoua cifrele. ⚠⚠ REPARATA in aceeasi zi: `comenzi_cu_transport_oferit` numara ORICE comanda cu transport zero — masurat pe demo, 26 din 31 erau ale unor coduri de procent sau suma fixa, unde transportul a iesit zero din pragul magazinului sau din ridicare personala. Acum se numara doar la codurile `free_shipping`. Gasita de criticul de completitudine, nu de mine. |
| 31 | `migrations/2026-09-21-discounturi-programare.sql` | `discounts.starts_at`; ziua din formular devine o ZI ROMANEASCA (inceput/sfarsit de zi, ora Romaniei); `claim_discount_use` capata in CHIAR instructiunea ei `is_active`, `starts_at` si `expires_at`; se STERGE `increment_discount_uses` | NU | Nu inca. ⚠⚠ Trei lucruri deodata, si doua sunt reparatii. **(1)** Coloana lipsea, desi ecranul o astepta: starea „Programat" era scrisa in `stare.ts` si optiunea era in filtru, dar nu se putea aprinde niciodata. **(2)** Ziua ajungea in baza ca miezul noptii UTC, adica 03:00 ora Romaniei in chiar ziua ei: cine scria „tine pana pe 31 august" pierdea 21 de ore. Masurat pe productie inainte de atins: 14 coduri, doua cu data, unul singur viu (`ANCA7`, tonel-beauty, expira 14.10.2027, zero utilizari) — el castiga 21 de ore, peste treisprezece luni. **(3)** Revendicarea verifica doar plafonul, deci un cod stins de comerciant intre validare si revendicare se revendica oricum. ⚠⚠ Si `increment_discount_uses` se sterge: crestea contorul FARA nicio conditie, era `security definer` si data lui `service_role`, deci ocolea din start orice regula noua. Masurat inainte de stergere: zero apelanti in `src/`, zero in corpul oricarei alte functii, zero declansatoare. ⚠ Semnatura lui `claim_discount_use` NU se schimba, deci granturile raman; masurat dupa aplicare pe demo: `postgres=X | service_role=X`, o singura versiune. ⚠ Probata pe baza demo cu un cod de unica folosinta, in tranzactie intoarsa: programat pentru maine = refuzat, pornit acum o ora = acceptat, expirat ieri = refuzat, oprit = refuzat, epuizat = refuzat, fara margini = acceptat. |
| 32 | `migrations/2026-09-21-discounturi-un-om-o-data.sql` | `normalize_phone` devine IDEMPOTENTA; `discount_customer_key` (doua argumente); `discounts.per_customer_limit`; tabela `discount_customer_uses` cu index UNIC; `orders.discount_use_id`; `reserve_discount_for_customer`; `claim_discount_use` capata telefon si email si intoarce MARCA; `release_discount_claim` inlocuieste `release_discount_use`; `release_order_discount` si `reclaim_order_discount` misca si registrul | NU | Nu inca. ⚠⚠ **NU SE POATE NUMARA DIN `orders`**, oricat de mult ar sta numaratoarea in WHERE-ul revendicarii, si din doua motive independente: (1) la clipa revendicarii comanda INCA NU EXISTA (`order.actions.ts:1887` fata de `:1914`); (2) zavorul e randul din `discounts`, iar subinterogarea peste `orders` se citeste pe instantaneul de la inceputul instructiunii. De-aia utilizarea se REZERVA intr-un registru cu index unic pe `(discount_id, customer_key, ordinal)` — indexul unic e singurul lucru care serializeaza doua tranzactii care nu ating acelasi rand existent. ⚠ **PROBAT CU CURSA ADEVARATA** pe baza demo, cu cereri paralele: 10 deodata pe limita 1 → 1 reusita; 12 pe limita 3 → 3; **25 pe limita 1 → 1**; 5 pe limita 5 → 5 (deci reluarea nu refuza pe nedrept). ⚠⚠ **REPARATIE DE TEMELIE**: `normalize_phone` taia UN SINGUR zero, deci `0722…` / `00722…` / `000722…` erau trei oameni deosebiti din acelasi telefon — „o data per client" se trecea adaugand un zero. Masurat pe productie inainte: se schimba DOUA chei din 541 de comenzi (un numar german si telefonul-fantoma `0000000000` al unei comenzi eMAG) si ZERO din cele 1.592 de randuri `customers`. ⚠⚠ `customers.key` e coloana GENERATA **STORED** cu aceeasi formula, iar Postgres NU o recalculeaza la schimbarea functiei: migratia **isi dovedeste singura premisa** si se OPRESTE daca vreun rand ar capata alta cheie. ⚠ Indexul `idx_orders_business_normphone` e scris pe functie, deci se face `reindex`. ⚠ `claim_discount_use` schimba semnatura si tipul intors: forma veche se STERGE pe fata, granturile se refac, PostgREST se reincarca. ⚠ Al doilea drum, `reclaim_order_discount` (desfacerea unei anulari), foloseste ACEEASI rezervare dar **fara portile de calendar**: comanda exista deja si a fost platita, iar o campanie incheiata intre timp n-are de ce s-o opreasca. |
| 33 | `migrations/2026-09-21-discounturi-pe-produse-si-categorii.sql` | `discounts.restrangere jsonb` si `orders.discount_base jsonb` | NU | Nu inca. ⚠ DOAR ADAUGARE, niciun corp de functie rescris — de-aia nu conteaza unde se aseaza in dosar, spre deosebire de **32**. ⚠⚠ `restrangere` are ACEEASI FORMA ca `offers.trigger`, dinadins: potrivirea se face cu CHIAR functia ofertelor (`extindeCategoriile`), fiindca ele raspund deja la „e produsul asta in categoria asta?" in acelasi checkout. Doua raspunsuri deosebite la aceeasi intrebare ar fi fost cel mai urat fel de defect. ⚠⚠ Categoriile se tin pe NUME fiindca `products.category` e un NUME si nu exista tabela de legatura; urmarile (redenumirea scoate produsele din campanie; doua categorii cu acelasi nume sub parinti deosebiti se prind amandoua) sunt scrise pe ecran, langa camp. ⚠⚠ `discount_base` pastreaza pe ce s-a socotit cuponul, fiindca dupa aceea nu se mai poate afla. E pregatita pentru factura — unde o reducere fara cota proprie se imparte PROPORTIONAL peste toate cotele — dar **facturarea inca NU o citeste**, si asta e o datorie scrisa: masurat, ZERO comenzi din 541 poarta cota pe linie, deci impartirea proportionala nu ruleaza niciodata azi. |
| 34 | `migrations/2026-09-22-discounturi-prima-comanda.sql` | `discounts.doar_prima_comanda`; index pe `(business_id, order_customer_key(...))`; `reserve_discount_for_customer` si `claim_discount_use` rescrise | NU | Nu inca. ⚠ **DATA DIN NUME E A ZILEI URMATOARE, DINADINS**: fisierul rescrie `claim_discount_use`, deci trebuie sa se aseze DUPA **32**; dosarul n-are numere de ordine, iar „prima-comanda" s-ar fi asezat inaintea lui „un-om-o-data" si pe o baza refacuta din dosar regula ar fi fost stearsa in tacere. ⚠⚠ „Prima comanda" inseamna de la sine O SINGURA folosire per om: altfel doua comenzi trimise deodata la primul cumparat ar fi vazut amandoua zero comenzi de dinainte. Limita lucratoare cade pe 1 chiar cand `per_customer_limit` e gol. ⚠ Intrebarea despre trecut se pune INAINTE de orice scriere, ca refuzul sa nu lase nimic de compensat. ⚠ Se numara ORICE comanda, si cele anulate. ⚠ `reclaim_order_discount` NU reintreaba: acolo omul are deja comanda. ⚠ Indexul e pe o expresie (`order_customer_key` e `immutable`) — fara el, intrebarea ar fi fost o parcurgere a intregii tabele de comenzi la fiecare plasare cu un cod de bun venit. ⚠ Probat pe demo, in tranzactie intoarsa: om cu comenzi = REFUZ, om nou = OK, acelasi om a doua oara = REFUZ, fara identitate = REFUZ, `uses_count` = 1. |
| 35 | `migrations/2026-09-22-discounturi-z-paginare.sql` | `discount_state`; `discounts_page` (filtru, sortare, paginare si `count(*) over ()` intr-un drum); `discount_state_counts`; `discount_totaluri`; index pe `(business_id, created_at desc)` | NU | Nu inca. ⚠⚠ Cerut de el dupa ce a intrebat daca sectiunea are paginare: „trebuie rezolvat de acum pentru viitor". Pana atunci pagina aducea TOATE codurile, fara `limit` — iar marginea adevarata era plafonul PostgREST, o mie de randuri, dupa care lista s-ar fi TAIAT IN TACERE. ⚠⚠ Aduce regula de stare in DOUA copii (`discount_state` si `stareaCodului`), lucru ocolit dinadins pana acum; sunt lipite de `starea-e-aceeasi-si-in-baza.test.ts`, care compara ordinea SI conditiile. ⚠ Cifrele din cap se socotesc pe TOT magazinul (`discount_totaluri`): adunate din lista adusa, ar fi SCAZUT cu fiecare pagina rasfoita. ⚠ Cifrele de langa filtre se numara PESTE CAUTARE. ⚠ Cautarea escapeaza `%` si `_`. ⚠ Asezarea alfabetica e `public.ro_numeric`, cea a casei — cu numere, deci „VARA2" inaintea lui „VARA10". ⚠ `security invoker`: granita e RLS-ul de pe `discounts`. ⚠ Probat pe demo cu 161 de coduri facute anume: „1–25 din 161", „1 / 7", `?page=2` da „26–50", `?stare=epuizat` da 25 de randuri toate scrise „Epuizat". |
| 36 | `migrations/2026-09-22-oferte-lista-si-cifre.sql` | `offer_state`; `offers_page` (filtru, sortare, paginare si `count(*) over ()` intr-un drum); `offer_state_counts`; `offer_totaluri`; index pe `(business_id, created_at desc)` | NU | Nu inca. ⚠ Aceeasi forma ca **35**, pentru Oferte: pagina aducea TOATE ofertele (`listOffers`, fara `limit`), iar marginea adevarata era tot plafonul PostgREST. Masurat pe productie la 22.09.2026: 13 oferte in 4 magazine, cea mai plina vitrina are 7. ⚠⚠ **PATRU stari, nu cinci**: la coduri exista si „epuizat", fiindca un cod are `max_uses`; o oferta n-are plafon de utilizari, deci optiunea aia n-ar fi cazut niciodata pe nimic. Probat ca asa ramane: `starea-ofertei-e-aceeasi-si-in-baza.test.ts` cere ca SQL-ul sa nu pomeneasca nici `epuizat`, nici `max_uses`. ⚠ Regula de stare e in doua copii (`offer_state` si `stareaOfertei`), lipite de aceeasi proba, care compara ordinea SI conditiile. ⚠⚠ `offer_totaluri` intoarce si `comenzi_cazute` / `bani_dati_cazuti` — din COMENZI, nu din contoare: `offers.impressions/conversions/revenue_added` sunt contoare care doar cresc si NU scad la anulare, fiindca `orders` nu pastreaza nicio legatura catre oferta folosita. Cifra aia e singurul fel de a spune pe ecran cat de mult nu scad; cand e zero, randul nici nu apare. ⚠ Cautarea prinde si CHEIA tipului („order_bump"), nu eticheta romaneasca: eticheta traieste in TypeScript, si scrisa si in SQL ar fi fost a doua copie. ⚠ `security invoker`; probat pe demo cu un JWT de authenticated: proprietarul vede 9 oferte, un strain vede 0. Cautarea cu `%` da 1 rand (cel cu „10%" in nume), nu toate noua — deci escaparea tine. |
| 37 | `migrations/2026-09-23-oferte-stoc-si-cele-mai-vandute.sql` | `offer_stoc`; `offers_page` si `offer_totaluri` RESCRISE cu starea de stoc; coloana `offers.fara_stoc_anuntat_la`; `oferte_de_anuntat_fara_stoc`; `produse_vandute`; index partial pe `orders` | NU | Nu inca. ⚠⚠ **DATA E A ZILEI URMATOARE, dinadins**: rescrie `offers_page`, deci trebuie sa se aseze DUPA **36** pe o baza refacuta din dosar. O proba cere chiar asta. ⚠⚠ **`create or replace` NU poate schimba tipul intors** — prima aplicare a CAZUT cu „cannot change return type of existing function". Se sterg intai, si stergerea ia cu ea GRANTURILE, deci se refac; plus `notify pgrst` ca PostgREST sa nu tina minte forma veche. ⚠⚠ `offer_stoc` numara STOCUL, nu „se poate lua dintr-o apasare": `needsChoice` (variante, personalizare) sta in `products.page_sections`, un jsonb pe care SQL nu-l citeste. Deci `ramase` e o margine DE SUS, si se spune pe fata. ⚠ Nu numara `volume` (nu ofera produse) si nici recomandarile automate (n-au lista fixa). Si cantitatile conteaza: un stoc de unu nu mai ajunge pentru un set care cere doua. ⚠⚠ `produse_vandute` desface `orders.items` — **NU exista tabela de linii de comanda**. Masurat: 544 de comenzi pe toata platforma, 276 la cel mai mare magazin, deci la scara asta e nimic; unde se rupe e scris in migratie. Fara cache dinadins: un agregat n-ar fi putut deosebi „zero vanzari" de „inca n-am socotit". ⚠ Comenzile anulate NU se numara: „cel mai vandut" pe anulari ar fi recomandat tocmai ce au refuzat oamenii. |
| 38 | `migrations/2026-09-23-conturi-clienti-temelia.sql` | CONTURI DE CLIENT, migratia 1: cele opt tabele din `privat` (`cont_cumparator`, `cont_contact`, `cont_sesiune`, `cont_comanda`, `cont_cod`, `cont_contact_blocat`, `cont_jurnal`, `cont_instiintare`), indexurile lor, blocul de drepturi NOMINAL cu verificare, si inchiderea lui `customer_orders` pentru `anon` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠ DOAR ADAUGARE de tabele noi, plus O SINGURA atingere a ceva existent: revocarea lui `customer_orders`. ⚠⚠ Revocarea se face de la `public`, NU doar de la `anon`: ACL-ul citit pe 23.09.2026 era `{=X/postgres, postgres=X, anon=X, authenticated=X, service_role=X}`, iar `=X` din fata e CHIAR `PUBLIC`, din care `anon` mosteneste oricum. Un `revoke from anon` singur ar fi parut ca lucreaza si n-ar fi inchis nimic. ⚠ `authenticated` PASTREAZA dreptul, fiindca functia e chemata de panoul comerciantului (`customer.actions.ts:23`); revocata si de acolo, modalul de istoric al clientului s-ar goli pentru toti comerciantii. Verificat dupa aplicare: `anon` nu mai poate, `authenticated` poate. ⚠⚠ Cheile straine catre cont sunt COMPUSE `(cont_id, business_id)`, ca baza sa opreasca singura o sesiune a magazinului A legata de un cont al magazinului B. **Probat pe demo, in tranzactie intoarsa**: perechea buna intra, perechea gresita cade cu `foreign_key_violation`, telefonul gol cade cu `check_violation`, si dupa rollback raman zero randuri. ⚠ Schema `privat` nu e un zid: masurat, `anon` si `authenticated` au `USAGE` pe ea. Singurul zid sunt granturile pe tabela, si `privat` nu are nicio intrare in `pg_default_acl`, deci o tabela fara bloc de drepturi e de neatins si pentru `service_role`. De-aia blocul e nominal si migratia se opreste singura daca a sarit vreuna. |
| 39 | `migrations/2026-09-23-conturi-clienti-setarea.sql` | CONTURI DE CLIENT, migratia 2: coloana `cont_client_config` pe `privat.store_settings`, plus refacerea vederii SI a declansatorului | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠⚠ TREI pasi, nu unul: fara `reconstruieste_store_settings()` coloana nu se vede prin vedere, iar fara `reconstruieste_store_settings_upd()` declansatorul INSTEAD OF rescrie randul INTREG cu lista VECHE de coloane si valoarea coloanei noi se pierde TACIT la fiecare salvare a oricarei ALTE setari. **Probat pe demo, in tranzactie intoarsa**: am scris `cont_client_config`, apoi am salvat prin VEDERE cu totul alta setari (`gpsr_config`), si valoarea a supravietuit. Migratia isi dovedeste singura ca vederea poarta coloana si se opreste daca nu. |
| 40 | `migrations/2026-09-23-conturi-clienti-sesiunea.sql` | CONTURI DE CLIENT, migratia 3: `cont_sesiune_creeaza`, `cont_sesiune_verifica`, `cont_sesiune_roteste`, `cont_sesiune_incheie`, `cont_iesi_de_peste_tot`, plus `privat.cont_reguli_sesiune` (30 de zile absolut, 14 de inactivitate, rotire la 24 de ore, gratie de 30 de secunde) | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠ Capetele stau in `public` fiindca PostgREST NU expune `privat`, deci o functie scrisa acolo n-ar putea fi chemata niciodata din cod. Toate sunt `security definer` cu `search_path` gol si date NUMAI lui `service_role`; verificarea e in chiar migratie. ⚠ `create or replace` REFACE granturile implicite (PUBLIC primeste EXECUTE), de-aia blocul de `revoke` sta la final. ⚠⚠ **Probat pe demo, sase pasi**: sesiunea proaspata e valabila; stinsa din Setari accesul se inchide pe loc FARA sa se stearga randul (H3); reaprinsa redevine valabila; rotirea omoara jetonul vechi; jetonul vechi prezentat dupa gratie ridica epoca si omoara TOATE sesiunile contului; dupa aceea si jetonul bun e mort. ⚠ Suspendarea magazinului NU e aici: regula ei traieste in TypeScript, in doua locuri, si a treia copie ar fi fost o copie a unei reguli de bani. |
| 41 | `migrations/2026-09-23-conturi-clienti-codul.sql` | CONTURI DE CLIENT, migratia 4: `cont_cere_cod` si `cont_verifica_cod` (cod de sase cifre pe email sau SMS), plus `privat.cont_normalizeaza` si `privat.cont_inceputul_zilei` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠⚠ Normalizarea se face NUMAI in baza: in TypeScript sunt TREI `normalizePhone` care raspund diferit. **Probat pe demo, opt pasi**: `0722 334 455`, `+40722334455` si `0040722334455` dau aceeasi destinatie si ACELASI cont (unul singur, nu trei); codul gresit e refuzat si creste contorul; al cincilea cod in 15 minute e refuzat; un contact blocat nu primeste cod; un telefon fara cifre nu primeste cod. ⚠ Plafoanele stau IN BAZA, nu numai in `consumaLimita`, fiindca limitatorul durabil cade DESCHIS la eroare de baza. ⚠ Bugetul de SMS e pe ZIUA ROMANEASCA, nu UTC. ⚠⚠ Un `42702: column reference "destinatie" is ambiguous` a cazut la prima proba: parametrul de iesire se cheama la fel cu coloana, deci UPDATE-ul are nevoie de alias. |
| 42 | `migrations/2026-09-23-conturi-clienti-citirile.sql` | CONTURI DE CLIENT, migratia 5: `cont_maturare` (leaga comenzile de cont pe contactele verificate), `cont_comenzile_mele` si `cont_comanda_mea` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠⚠ Regula de marketplace e scrisa cu `not coalesce(order_source ? 'marketplace', false)`. Forma fara `coalesce` arunca TACIT cele 64 de comenzi cu `order_source` NULL. ⚠ Lista alba de coloane e in `returns table` si COBOARA si in `items`, fiindca `items[].customization.value` tine chei R2, iar fisierele de dinainte de 07.09.2026 stau in galeata publica. ⚠ Perechea curier-AWB-urmarire e scrisa O SINGURA DATA, intr-un `values` lateral: un `case` pentru curier si un `coalesce` separat pentru numar s-ar fi despartit si ar fi aratat AWB-ul unui curier sub numele altuia. ⚠ `cont_comanda_mea` se face cu `drop`+`create`, nu `create or replace`: lista de coloane s-a schimbat si Postgres da `42P13`; granturile se refac de mana. ⚠⚠ **Probat pe date demo**: o comanda de MARKETPLACE pusa pe acelasi telefon NU se leaga; comanda nu se deschide cu alt `cont_id`; liniile nu poarta campuri din afara listei albe; cheia de EMAIL chiar leaga comenzi, spre deosebire de `customer_orders`. |
| 43 | `migrations/2026-09-23-conturi-clienti-reparatii.sql` | CONTURI DE CLIENT, migratia 6: reparatiile gasite de sase sceptici peste Etapele A si B. Validarea emailului in `cont_normalizeaza` plus `check` pe `cont_cod.destinatie`; `cont_cod.ip` si plafonul pe (magazin, IP) in BAZA; codurile vii nu se mai omoara intre ele si `cont_verifica_cod` primeste oricare din ele; buget zilnic si pentru EMAIL; `cont_comanda.vedere` cade implicit pe `redusa`; `cont_comanda_mea` inchide cele sase campuri care treceau pe langa poarta; `motiv_incheiere` capata `expirare` si `inactivitate`; chei straine pe `business_id` la `cont_jurnal` si `cont_instiintare`; index pe `(business_id, lower(btrim(customer_email)))`; `cont_curatenie()` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA 38-42. ⚠⚠ Cea mai grava era o ARMA, nu o scurgere: plafonul de coduri era cheiat numai pe destinatie si fiecare cerere noua OMORA codul viu al destinatarului, deci un strain care stia adresa unui client il tinea afara din cont la nesfarsit, iar doctrina „un singur raspuns" facea totul invizibil. **Probat pe demo**: codul victimei supravietuieste cererii strainului; plafonul pe IP din baza opreste la a 16-a cerere; o lista de adrese si o injectie de antet cad amandoua; vederea redusa nu mai scurge niciunul din cele unsprezece campuri; presetarea lui `vedere` e `redusa`. ⚠ `cont_comanda_mea` si cele doua functii de cod se fac cu `drop`+`create` (semnaturi schimbate), cu granturile refacute si verificate in migratie. |
| 44 | `migrations/2026-09-23-conturi-clienti-preferinte.sql` | CONTURI DE CLIENT, migratia 7: `cont_preferinte` si `cont_preferinte_schimba`, peste `recovery_optout` si `sms_optout` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠ Ecranul nu adauga o promisiune, o face adevarata pe cea DEJA publicata: sablonul de confidentialitate al fiecarui magazin scrie dreptul de opozitie, iar pana acum singurul drum era linkul de dezabonare dintr-un email pe care omul putea sa nu-l mai aiba. ⚠⚠ Lucreaza NUMAI pe contactele VERIFICATE: altfel cineva si-ar fi scris in cont adresa altui om si i-ar fi oprit mesajele. ⚠ Comparatia telefonului trece prin `normalize_phone` pe AMANDOUA partile: randurile vechi din `sms_optout` pot fi scrise altfel, iar o potrivire pe sirul brut ar fi spus „primesti SMS-uri" unui om care raspunsese STOP. ⚠ Nu se tine o a doua evidenta: adevarul ramane in tabelele pe care le citesc deja cronul de recuperare si campaniile. |
| 45 | `migrations/2026-09-23-conturi-clienti-datele-si-stergerea.sql` | CONTURI DE CLIENT, migratia 8: `cont_contactele_mele`, `cont_sterge_contact`, `cont_retururile_mele`, `cont_anuleaza_comanda`, `cont_export`, `cont_sterge` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠⚠ ULTIMUL contact verificat nu se poate scoate: nu exista parola, deci contactele SUNT singura cale de intrare, iar omul s-ar incuia singur afara pentru totdeauna. Numaratoarea se face excluzand chiar contactul care se sterge, in aceeasi instructiune. ⚠⚠ IBAN-ul returului se mascheaza IN SQL, nu in componenta: o mascare la randare se ocoleste de a doua randare, de export sau de urmatorul ecran. ⚠⚠ Anularea trece prin `aplica_tranzitia_comenzii`, niciodata printr-un `update` pe `status`: acolo stau eliberarea stocului, desfacerea cuponului si cele doua declansatoare de email marketing. Numai la `pending`, niciodata la marketplace. ⚠⚠ Stergerea contului NU sterge comenzile (venitul lunilor incheiate ar scadea retroactiv si facturile ar ramane fara nimic in spate) si NU atinge dezabonarile. Contactele SE sterg, deci omul poate deschide alt cont maine cu aceeasi adresa. **Probat pe demo, patru pasi**: ultimul contact refuzat, dezabonarea scrisa in tabela adevarata, o comanda livrata nu se anuleaza iar una `pending` da, si dupa stergere raman toate comenzile si toate dezabonarile. ⚠ `cont_retururile_mele` leaga prin `order_id`, care e NULABIL si `on delete set null`: un retur a carui comanda a fost stearsa nu apare, si e o gaura veche a schemei, scrisa acum si in comentariul functiei. |
| 46 | `migrations/2026-09-23-conturi-clienti-panoul.sql` | CONTURI DE CLIENT, migratia 9: `cont_cate_conturi`, pentru fila din Setari | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului. ⚠ Exista fiindca tabelele contului stau in `privat`, pe care PostgREST nu o expune: un `count` din panou n-ar avea ce citi, iar „reparatia" evidenta (mutarea lor in `public`) ar fi desfacut chiar zidul pentru care sunt acolo. ⚠ Numara doar conturile VII. |
| 47 | `migrations/2026-09-23-conturi-clienti-banii-comenzii.sql` | CONTURI DE CLIENT, migratia 10: `cont_comanda_mea` intoarce si `reducere_card`, `reducere_ramburs`, `cod_reducere`, `tva`, `cota_tva` si `regim_tva` (regimul de pret INGHETAT pe comanda) | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **43** (rescrie functia pe care o rescrie si 43). ⚠⚠ Gasit verificand pe demo ce vede contul de proba: pe 3 din cele 6 comenzi randurile nu dadeau totalul, iar ecranul punea diferenta pe „Alte ajustari”. Coloana se aduna, dar banii erau fara nume. **Masurat pe PRODUCTIE, doar citire:** din 344 de comenzi de vitrina, **41** ar fi aratat „Alte ajustari”: 38 cu reducerea pentru plata cu cardul (3 magazine), 1 cu TVA adaugat peste pret (regim inghetat `false`) si cele 2 comenzi Medclean din 9 iunie cu TVA-ul adunat de doua ori (acolo randul ramane, dinadins). Liniile dau `subtotal + extraoptiuni` pe TOATE 344. ⚠ `drop`+`create` (lista de coloane schimbata, altfel `42P13`), granturile refacute si verificate in migratie; ACL citit dupa aplicare: `{postgres=X, service_role=X}`. ⚠ Toate campurile noi sunt imbracate in `case when l.vedere = 'intreaga'`. **Probat pe demo, in tranzactie intoarsa**: trecuta pe `redusa`, comanda #1352 intoarce NULL pe toate sase, iar dupa rollback toate 6 comenzi sunt tot pe `intreaga`. ⚠ `offer_discount_amount` NU intra: reducerea din oferta e deja in pretul liniilor (#1356), nici caseta din panou nu o scade. |
| 48 | `migrations/2026-09-23-conturi-clienti-comanda-completa.sql` | CONTURI DE CLIENT, migratia 11: comanda cu toate detaliile si facturile. Ajutoare noi in `privat`: `cont_e_document_de_test`, `cont_documentul_comenzii` (O SINGURA regula pentru documentul fiscal), `cont_personalizarea_liniei`, `cont_defalcarea_liniei`. RESCRISE cu `drop`+`create`: `cont_comanda_mea`, `cont_comenzile_mele`, `cont_retururile_mele`. NOI: `cont_facturile_mele`, `cont_rezumat` | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **47**. ⚠⚠ Trei defecte reparate, toate masurate: **(1)** codul postal nu iesea NICIODATA (toate definitiile citeau `postcode`, cheie cu 0 randuri; checkout-ul scrie `postal_code`, 189 pe productie, si mai vechi `postalCode`, 74); **(2)** factura STORNATA aparea valabila (stornarea lasa numarul facturii pe rand; 25 SmartBill + 3 fGO pe productie); **(3)** documentul fGO de TEST aparea ca factura (2 comenzi pe productie). ⚠ Liniile capata `extra`, `imagine` si `slug` (catalogul de azi, join purtand si magazinul, `product_id` transformat in uuid numai cand arata a uuid), plus `personalizare` si `defalcare` imbracate in vederea intreaga, fara NICIO cheie de fisier (se da doar numarul fisierelor). Livrarea capata tara, curierul ales (ridicarea personala e `courier=pickup` cu `delivery_type=address`), eticheta aleasa la checkout si adresa punctului; firma capata sediul si platitor de TVA; iese si `payment_status`, `offer_discount_amount`, `notes` si data AWB-ului. ⚠⚠ Lista de documente de TEST exista in DOUA copii (SQL si `GAZDE_DE_TEST`); o proba le compara. ⚠ `cont_retururile_mele` capata `order_id` si produsele, si NOU `l.vedere = 'intreaga'`: pana acum un retur pe o comanda legata doar pe numar arata motivul si IBAN-ul mascat. ⚠ `bucati` din lista era numarul de LINII; acum e suma cantitatilor fara extraoptiuni. **Probat pe demo**: codul postal apare (900127, 021045, 330395); #1353 are 6 campuri de personalizare, fotografia doar ca „1 fisier”; rezumatul contului de proba da 12 comenzi, 7 in curs, 5 facturi, 2 retururi; #1310 iese stornata cu nota CLM 0225; fGO de test (#1004) nu apare nicaieri. Granturi verificate in migratie. |
| 49 | `migrations/2026-09-23-conturi-clienti-obligatoriu-si-curier.sql` | CONTURI DE CLIENT, migratia 12: contul OBLIGATORIU la comanda si curierul real al coletului. Temei nou `plasata-in-cont` in `privat.cont_comanda`; NOI: `cont_leaga_comanda_plasata`, `cont_buget_email_epuizat`, `cont_intrari_recente`; RESCRISA cu `drop`+`create`: `cont_comanda_mea` (+ `curier_real`) | **DA, 23.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **48**. ⚠⚠ ORDINEA E SFANTA: codul cere `cont_client_config` (coloana din **39**) in AMBELE actiuni de comanda si in antetul vitrinei; fara migratii, interogarea de setari cade INTREAGA si comenzile nu se mai pot plasa deloc. Deci 38-49 in productie, abia apoi codul. ⚠ Legarea la plasare: comanda magazinului dat, de vitrina (regula cu `coalesce`), mai noua de 10 minute, cont viu, `on conflict do nothing`; se cheama dupa insert si nu poate rupe comanda. ⚠ Bugetul epuizat se numara EXACT ca in `cont_cere_cod`. ⚠ `curier_real`: masurat pe productie, 273 din 274 de AWB-uri Woot sunt DPD dedesubt; iese din prefixul lui `woot_service_name`, pe ambele separatoare masurate (semnul lung, punctul median). Tiparul DPD a iesit din SQL: adresa de urmarire se compune numai in `src/lib/cont/urmarire.ts`. Granturi verificate in migratie (numai `service_role`). |
| 50 | `migrations/2026-09-24-conturi-clienti-parola.sql` | CONTURI DE CLIENT, migratia 13: EMAIL SI PAROLA. Coloane noi: `cont_cumparator.parola_hash` + `parola_schimbata_la`, `cont_cod.provocare_hash` + `parola_hash`; scopuri noi de cod (`inregistrare`, `resetare-parola`, `doi-pasi`); tabela noua `privat.cont_dispozitiv`; NOI: `cont_retrimite_cod`, `cont_verifica_provocare`, `cont_parola_pentru_intrare`, `cont_intrare_esuata`, `cont_intrare_cu_parola`, `cont_dispozitiv_cunoscut`, `cont_dispozitiv_adauga`, `cont_parola_contului`, `cont_schimba_parola`, `privat.cont_ip_sigur`; RESCRISE: `cont_cere_cod` (drop+create, 2 parametri noi), `cont_sterge`, `cont_curatenie` (drop+create, coloana `dispozitive`) | **DA, 24.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **49**. Cerut de el: „utilizatorul sa se conecteze cu Email si Parola, iar cine isi face cont sa-l verifice cu 2FA prin eMail”. ⚠⚠ In baza ajunge NUMAI amprenta scrypt (calculata in Node); doua constrangeri refuza orice nu incepe cu `scrypt$`. ⚠⚠ Intrarea NUMAI cu cod se INCHIDE: `cont_cere_cod` refuza scopul `intrare`. ⚠ Fiecare cod de cont nou, resetare sau pas doi e legat de o PROVOCARE (amprenta unui jeton din cookie-ul `httpOnly` `ec_cont_pas`): codul din email nu deschide nimic in alt browser. ⚠ Plafoanele parolei stau in baza: 5 greseli / 15 min pe cont (de la ultima intrare reusita sau schimbare de parola), 20 / 15 min pe IP la magazin. ⚠ `cont_sterge` goleste acum si parola, dispozitivele, sesiunile (poarta IP-uri) si instiintarile; curatenia sterge si sesiunile EXPIRATE neatinse (pana acum ramaneau pe veci, cu IP). **Probat pe demo, in tranzactii intoarse**: 25 de cazuri (scop inchis, cod gresit/strain/refolosit, cont nou, blocaj la 5 si deblocare dupa o intrare, IP blocat la 20 fara sa atinga alt IP, pas doi pe alt cont refuzat, retrimitere si provocare incheiata, resetare fara parola care NU consuma codul, epoca +1, dispozitive sterse, parola in clar refuzata de constrangere). Corpurile celor 15 functii din baza au aceeasi amprenta md5 ca fisierul (fara comentarii si spatii). Granturi verificate in migratie (numai `service_role`). ⚠⚠ **Intarita dupa verificarea din 24.09.2026** (patru recenzenti independenti, apoi reaplicata pe demo): resetarea pe o adresa FARA cont scrie un rand-MOMEALA, deci verificarea raspunde „gresit” si retrimiterea merge, ca la o adresa cu cont (inainte era un oracol: „a expirat” si 400); codurile pasului doi se numara SEPARAT de cele cerute de oricine si nu le opreste plafonul zilnic (un strain nu mai poate bloca intrarea cuiva); ghicitul codului: 10 incercari pe zi pe adresa, peste toate provocarile; numaratorile si scrierile sub `pg_advisory_xact_lock` pe destinatie (nu mai pot fi intrecute de cereri deodata); orice schimbare de parola inchide codurile inca vii; blocajul pe IP si cel pe cont vin separat (pe un dispozitiv de incredere blocajul pe cont nu se aplica); `cont_verifica_cod` pierde ramura veche care CREA conturi fara parola; `cont_sterge` sterge si jurnalul si codurile de pe adresele contului; curatenia sterge si instiintarile netrimise de o luna. **Probat pe demo, intors**: momeala (1a-1e), cont existent (`trimis-cont-existent`), pasul doi neatins de coduri straine, coduri inchise la schimbarea parolei, 10/zi (a 10-a greseala opreste si codul bun), blocaje separate, adresa stricata refuzata fara exceptie. Toate cele 17 corpuri de functii au aceeasi amprenta ca fisierele; ambele migratii trec prin parserul Postgres (`libpg-query`, 20 de corpuri PL/pgSQL). |
| 51 | `migrations/2026-09-24-conturi-clienti-z-anonimizarea.sql` (cu `z-`, ca sa vina dupa 50 si in ordinea numelor) | CONTURI DE CLIENT, migratia 14: ANONIMIZAREA ajunge si in conturi. NOUA: `cont_rupe_legaturile` (`security definer`, data lui `authenticated`, cu paza de proprietar in corp). ⚠⚠ RESCRISA: `customer_anonymize`, FUNCTIE DE PRODUCTIE chemata azi de butonul „Anonimizeaza” (semnatura si coloanele intoarse NESCHIMBATE) | **DA, 24.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **50** (goleste si `cont_dispozitiv`/`parola_hash` din 50). Definitia de azi era aceeasi pe productie si pe demo (md5 `0867eeff…`, citit pe 24.09.2026). Repara capitolul 10 din plan: **(1)** contul omului (contacte, parola, sesiuni cu IP, dispozitive, jurnal) se goleste in aceeasi tranzactie, gasit dupa email, dupa telefon SI dupa comenzile legate; **(2)** `order_source`: lista NEAGRA ramane (hotararea din 21.09: cheile de bani se inmultesc cu fiecare marketplace), dar e completa: pleaca si `client_ip` (38 de comenzi pe productie il au), `fbc`, `ttp`, `mc_tc`, `ga_sesiuni`, `utm_term`, `utm_content`, acordurile; clicurile devin `anonimizat` (clasificarea „platit” ramane), referrer-ul ramane numai ca origine; **(3)** IBAN-ul si motivul se golesc NUMAI pe retururile `rambursat`/`respins` (pe `nou`/`aprobat` comerciantul trebuie inca sa ramburseze); **(4)** cosurile abandonate se gasesc si dupa email, nu doar dupa cheia telefon-intai. **Probat pe demo, intors**: strain refuzat (42501); proprietarul: 22 de comenzi, 4 retururi, contul de proba sters cu tot (0 contacte, 0 legaturi, 0 sesiuni, un singur rand de jurnal); o comanda Pepita si-a pastrat toate cheile de bani, inclusiv una necunoscuta. Proba noua leaga lista din SQL de `CHEI_ATRIBUIRE` din `order.actions.ts`. ⚠⚠ **Intarita dupa verificarea din 24.09.2026**: un cont se goleste NUMAI cand e sigur al omului (gasit dupa cheile lui sau randul din `customers`, ori dupa contactele comenzilor si fara alte comenzi legate); cine a trimis un cadou pe telefonul omului isi pastreaza contul, pierde doar legatura cu comanda aceea; SMS-urile pierd si TEXTUL (sabloanele pot purta numele si adresa), iar `notice_inbox` se goleste; in masa, fiecare cheie primeste adresa anonima a ei (inainte N clienti deveneau unul); curatarea sursei sta intr-o functie pura, `privat.sursa_anonimizata` (data si lui `authenticated`), folosita si de o curatare O DATA a comenzilor anonimizate inainte (pe productie: zero, la 24.09.2026). **Probat pe demo, intors**: contul cumparatorului de cadou ramas viu, legatura cu cadoul desfacuta, a lui ramasa; adrese anonime diferite pe doua chei; textul SMS golit. |
| 52 | `migrations/2026-09-24-conturi-clienti-zz-datele-mele.sql` (cu `zz-`, ca sa vina dupa 51 si in ordinea numelor) | CONTURI DE CLIENT, migratia 15: „DATELE MELE”, reparatiile din verificarea ceruta de el inainte de unire („functiile de la Datele mele cu SESIUNI, DESCARCA DATELE, ETC... FUNCTIONEAZA CUM TREBUIE?”). RESCRISE, cu aceeasi semnatura si acelasi tip intors: `cont_iesi_de_peste_tot` (sterge si `cont_dispozitiv`: inainte, telefonul pierdut ramanea „dispozitiv de incredere” 60 de zile si intra numai cu parola, fara cod), `cont_export` (TOATE comenzile, fiecare prin `cont_comanda_mea`, deci cu aceeasi vedere ca pe ecran; inainte se oprea la 100 si dadea numai numarul, data si totalul; plus `are_parola`, `parola_schimbata_la`, dispozitivele tinute minte; nicio amprenta), `cont_sterge_contact` (ultimul EMAIL confirmat nu se scoate, oricate telefoane ar avea contul: intrarea, resetarea si pasul doi merg numai pe email), `cont_verifica_cod` (adresa noua mosteneste „fara emailuri” din Preferinte; restul identic cu 50) | **DA, 24.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **51** (redefineste `cont_verifica_cod` din 50). Granturi verificate in migratie (numai `service_role`). **Probat pe demo, intors**: iesirea de peste tot (epoca 1->2, dispozitive 0); exportul cu 150 de comenzi legate (150 in fisier, cu adresa, plata, factura; fara `scrypt$`, fara jetoane, fara `parola_hash`/`cod_hash`); email+telefon: emailul refuzat, telefonul scos; email neconfirmat nu tine loc; doua emailuri confirmate: unul se scoate; adresa noua a unui dezabonat primeste randul in `recovery_optout`, a unui abonat nu; cod gresit si scop inchis ca inainte. Cele 4 corpuri au aceeasi amprenta (md5, fara comentarii si spatii) ca fisierul; trece prin parserul Postgres (`libpg-query`). |
| 53 | `migrations/2026-09-24-conturi-clienti-zzz-panoul-conturilor.sql` (cu `zzz-`, dupa 52 si in ordinea numelor) | CONTURI DE CLIENT, migratia 16: PANOUL CONTURILOR la comerciant (cerut de el: „comerciantul poate vedea toate conturile create in magazinul lui si detalii despre fiecare cont?”, apoi „fa-o complet si cu mare atentie”). Coloana noua `privat.cont_cumparator.suspendat_la`. NOI: `privat.cont_ip_ascuns` (IP pe jumatate: `86.124.x.x`, IPv6 pe /32), `cont_verifica_suspendarea` (chemata DUPA parola buna), `cont_panou_lista`, `cont_panou_sumar`, `cont_panou_fisa` (fara amprente, jetoane, coduri; IP ascuns), `cont_panou_suspenda` (ridica epoca, uita dispozitivele, inchide codurile vii; motivul sta in jurnal, deci pleaca la stergere si anonimizare), `cont_panou_reactiveaza` (epoca NU coboara), `cont_panou_iesire`, `cont_panou_sterge` (acelasi drum ca `cont_sterge`), `cont_panou_comanda_de_legat` / `cont_panou_leaga_comanda` / `cont_panou_dezleaga_comanda` (marketplace refuzat, fara mutare de la alt cont, dezlegare NUMAI pentru `legat-de-comerciant`), `cont_panou_chei` (cheia clientului cu `order_customer_key`/`discount_customer_key`, ca lista; comenzile legate de mana NU dau eticheta). RESCRISE: `cont_sesiune_creeaza` (+ `suspendat_la is null`: singura usa a sesiunilor noi) si `cont_verifica_provocare` (copiata mecanic din 50 + trei opriri `suspendat`: pasul doi, cont nou pe adresa cu cont, resetare; nimic altceva schimbat, apara o proba) | **DA, 24.09.2026** | **NU.** De aplicat la unire, INAINTEA codului, DUPA **52**. Granturi numai `service_role`, verificate in migratie. **Probat pe demo, intors**: lista (cautare dupa email, numar de comanda, `_` scapat, filtre), fisa fara secrete si cu IP ascuns, suspendarea (epoca 1->2, dispozitive 0, sesiuni 0, fara sesiune noua, pasul doi / resetarea / contul nou refuzate cu parola neschimbata), reactivarea, iesirea (1 sesiune inchisa), legarea (marketplace, alt cont, alt magazin refuzate; dezlegarea automata refuzata), cheia, stergerea. Cele 15 corpuri au aceeasi amprenta ca fisierul; `cont_panou_chei` reaplicat de doua ori dupa recenzie (legaturile de mana si comenzile-cadou nu dau eticheta; numai comenzile cu emailul sau telefonul unui contact confirmat al contului), amprenta verificata. Date demo: `demo-seed/91-conturi-panou.sql` (Irina activa, Cosmin suspendat, Elena fara comenzi). |

### ⚠⚠ Conturile de client (38-53) se aplica in ORDINEA TABELULUI, NU in ordinea numelor

Cele 12 fisiere din 23.09 sortate alfabetic ies cu totul altfel (`banii-comenzii` inaintea lui
`temelia`, care creeaza tabelele). Aplicate asa, primul fisier cade pe tabele care nu exista
inca. `z-`/`zz-` tin in ordine doar pe cele trei din 24.09. In productie, `rtefdpioqmowkdiybwrr`,
exact asa, unul cate unul, cu verificarea drepturilor dupa fiecare (C):

1. (38) `2026-09-23-conturi-clienti-temelia.sql`
2. (39) `2026-09-23-conturi-clienti-setarea.sql`
3. (40) `2026-09-23-conturi-clienti-sesiunea.sql`
4. (41) `2026-09-23-conturi-clienti-codul.sql`
5. (42) `2026-09-23-conturi-clienti-citirile.sql`
6. (43) `2026-09-23-conturi-clienti-reparatii.sql`
7. (44) `2026-09-23-conturi-clienti-preferinte.sql`
8. (45) `2026-09-23-conturi-clienti-datele-si-stergerea.sql`
9. (46) `2026-09-23-conturi-clienti-panoul.sql`
10. (47) `2026-09-23-conturi-clienti-banii-comenzii.sql`
11. (48) `2026-09-23-conturi-clienti-comanda-completa.sql`
12. (49) `2026-09-23-conturi-clienti-obligatoriu-si-curier.sql`
13. (50) `2026-09-24-conturi-clienti-parola.sql`
14. (51) `2026-09-24-conturi-clienti-z-anonimizarea.sql`
15. (52) `2026-09-24-conturi-clienti-zz-datele-mele.sql`
16. (53) `2026-09-24-conturi-clienti-zzz-panoul-conturilor.sql`

Pe demo s-au aplicat exact in ordinea asta. Dupa ultima: `notify pgrst, 'reload schema'` (e si in
fisier) si abia apoi codul.

### ⚠⚠ Cum au intrat 17-29 in productie (21.09.2026, seara)

Nu ca treisprezece fisiere rulate pe rand, ci ca **starea lor finala**, in cinci pasi
(`clienti_01_tabele` … `clienti_06_anon_fara_niciun_drept`). Motivul: sirul trece de mai multe
ori prin aceeasi functie, stergand-o si recreand-o — iar productia n-avea nevoie de drum, ci de
capat.

⚠ **Si de-aia dovada nu e „am rulat fisierele", ci o comparatie**: s-au luat amprentele
(`md5(pg_get_functiondef(...))`) celor **14 functii** de pe demo si s-au cerut aceleasi de pe
productie. Toate **identice**, cate o singura versiune din fiecare, fara semnaturi vechi ramase
in urma. `normalize_phone` si `order_customer_key` erau deja identice INAINTE, deci si punctul
de plecare era acelasi.

⚠ Verificat inainte de a atinge ceva: **nicio migratie nu scrie intr-o tabela cu date**. Toate
`insert`/`update`/`delete` din ele sunt in CORPUL unor functii (ruleaza la apel), iar singurele
`alter table` sunt pe cele trei tabele noi, goale.

⚠ Dupa aplicare, masurat pe productie: 538 de comenzi, 1.592 de randuri in `customers` si
35.156,76 lei — **neschimbate**. Si probat ca un comerciant adevarat: 269 de clienti, sumarul
corect, toate zece segmentele numarate, cronologia unui client cu 8 evenimente, iar cererea
datelor ALTUI magazin da **zero**.

⚠⚠ **Un drept lasat descuiat, gasit atunci**: dupa `revoke select, insert, update, delete`,
`anon` ramanea cu **TRUNCATE** pe cele trei tabele noi. TRUNCATE e singurul drept pe care RLS
NU-l filtreaza — nu se uita la randuri, goleste tabela. Nu era ajuns de nicaieri (PostgREST
n-are verb de TRUNCATE), dar toata paza sectiunii se sprijina pe „RLS e granita", iar acolo
granita nu exista. Inchis cu `revoke all`, pe amandoua bazele. ⚠ Acelasi lucru e adevarat
pentru toate tabelele mai vechi ale platformei (`customers` are si azi TRUNCATE pentru `anon`);
cer o trecere a lor.

⚠ Migratiile 1-3 sunt **numai citire**: functii noi si o politica de SELECT, niciun `alter table`,
niciun rand atins.

⚠ **Migratia 4 e prima care schimba o tabela**: adauga patru coloane la `site_analytics`, toate
optionale, plus doua indexuri si o tabela noua (`analitice_sare`). Nu atinge niciun rand existent
si nu strica scrierile de azi - codul vechi care insereaza fara coloanele noi ramane valid.
Indexurile se construiesc pe o tabela care creste cu fiecare vizita, deci la aplicare se face pe
rand, nu in acelasi minut cu push-ul.

### ⚠⚠ CODUL SI MIGRATIILE LUI S-AU DESPARTIT (21.09.2026)

**Ce s-a intamplat.** Codul filei Cosuri abandonate a plecat pe `main` in dimineata de
21.09 (deployul de la 10:42), iar `main` **E** productia. Migratiile 12-16 erau insa lasate
pentru „ziua unirii”, dupa regula de sus. Deci trei ore fila a fost RUPTA in productie
pentru 21 de magazine, si recuperarea automata n-a mai trimis nimic pentru magazinul care o
are pornita.

**Cum s-a vazut.** Nu dintr-o alarma: gasita citind chiar tabelul asta, la o verificare de
rutina. Nicio eroare n-a ajuns la nimeni, fiindca nimeni nu deschisese fila intre timp.

**Ce a mers bine, si merita spus.** Caderea a fost in directia SIGURA:

- apararea impotriva mesajelor duble nu e randul din `recovery_sends`, ci
  compare-and-swap-ul pe `automation_step`, care exista. **Niciun mesaj dublu, niciun ban
  cheltuit degeaba pe SMS.**
- cronul sare magazinul si scrie `critical`, in loc sa trimita orbeste.

**⚠ REGULA CARE LIPSEA, si care ramane:** cat timp se lucreaza direct pe `main`, o
migratie NU poate fi amanata pentru „final” daca un cod care o cere pleaca mai devreme.
Fie pleaca amandoua, fie niciuna. Regula de sus („productia nu se atinge pana la unire”)
a fost scrisa cand lucrul statea pe o ramura; pe `main` ea nu se mai poate tine, si a se
purta ca si cum s-ar putea e felul in care se ajunge exact aici.

### ⚠⚠ Migratia 11 a plecat INAINTE de final, si de ce

Regula e ca productia nu se atinge pana la unire. Aici s-a facut o exceptie, hotarata cu el:
`recovery_optout` avea numai `email`, deci **dezabonarea de la SMS nu se putea nici macar
exprima**, cu atat mai putin respecta. Iar trimiterea de mana din panou nu citea lista deloc.

⚠ Pe productie plecasera deja **34 de emailuri si 21 de SMS-uri** catre clienti adevarati, deci
gaura nu era teoretica. Un mesaj trimis cuiva care a cerut sa nu mai fie contactat nu se ia
inapoi, si nici banii pe SMS.

Migratia nu sterge nimic si nu atinge niciun rand: adauga doua coloane, slabeste un `not null`
si pune o restrictie care cere macar un contact. Codul vechi, care scria doar `email`, ramane
valid - `motiv` are implicit `'dezabonare'`.

### ⚠⚠ Migratia 3 repara si un defect care e ACUM in productie

`business_daily_stats` are RLS pornit si **zero politici**: in afara de `service_role`, nimeni
nu poate citi din ea. Toate functiile de statistici sunt `security invoker`, deci comerciantul
primeste doar vizitele de AZI (cele brute din `site_analytics`), niciodata zilele stranse.

Masurat pe baza demo, prin panoul real: pagina **Statistici** arata „Rata de conversie 250.0%,
38 vizite" pentru 30 de zile, cand luna avea 5.701 de vizite. Dupa politica: 5.701, iar rata
intra la loc sub 100%. Cardul nou de conversie a dat defectul de gol (arata 173,7%).

**Rezolvat in productie pe 20.09.2026**, cu acordul lui („rezolva asta daca e rapid si fara sa
strici nimic"), fiindca lovea zilnic cei 71 de comercianti care au statistici: 3.054 de randuri,
niciunul citibil de proprietarul lui.

Verificat dupa aplicare, chiar in productie, cu RLS pornit si dandu-ne drept comerciantul cu
cele mai multe zile (totul intr-o tranzactie inchisa cu `rollback`, fara nicio scriere):
isi vede toate cele 249 de zile ale lui, **zero** randuri de la alte magazine, iar `anon` vede
in continuare zero. Functia `panou_carduri` a ramas neaplicata: ea tine de redesign.

⚠ **RAMAS DE FACUT, si nu se poate din sesiunea asta:** schema de referinta din Git nu mai
cuprinde politica noua, fiindca regenerarea cere o legatura la baza, pe care mediul mi-a
refuzat-o („Modify Shared Resources"). Se ruleaza, de catre el sau cu permisiunea lui:

    bash scripts/schema-baseline.sh          # rescrie migrations/000-schema-baseline.sql
    bash scripts/schema-baseline.sh --check  # trebuie sa spuna ca Git = productia

Pana atunci, `--check` va semnala o diferenta: e chiar politica de mai sus, nu o surpriza.

---

## B1. ⚠⚠ O GAURA GASITA IN PRODUCTIE PE 21.09.2026, si inchisa pe loc

`migrations/2026-09-21-copii-de-siguranta-inchise.sql` - **aplicata in productie**, cu aceeasi
judecata ca migratia 11: era deschisa, si nu se putea astepta pana la unire.

Trei tabele de siguranta facute pe 20.09 aveau `row level security` OPRIT si drepturi catre
`anon`, adica erau de citit SI de scris de oricine are cheia publica a unui magazin - cheia aia
sta in bundle-ul fiecarei vitrine:

| Tabela | Randuri |
|---|---|
| `zz_backup_anunturi_stinse_20260920` | 6 |
| `zz_backup_esafe_page_sections_20260920` | **3.351** (continut de pagina al unui comerciant) |
| `zz_backup_feed_vetdepo_20260920` | 1 |

⚠ **NU era o gaura de cod, ci una de MANA.** Tabelele s-au nascut dintr-un `create table ... as
select` scris in consola, iar `create table as` nu mosteneste nici RLS-ul, nici granturile
tabelei din care copiaza. Cine salveaza ceva „doar pentru cinci minute" nu se gandeste la Data
API.

⚠ **Cum a iesit la iveala**, fiindca merita tinut minte: NU cautand-o. Schema de referinta din
Git era in urma, iar cand a fost regenerata, plasa `rls-tabele.test.ts` a vazut in sfarsit
tabele pe care inainte nu le avea in fisier. Plasa era buna de luni de zile; ii lipsea hrana.
De-aia „schema din Git = schema din productie" nu e o curatenie, ci o conditie ca probele sa
poata vedea.

S-au inchis toate cele douasprezece, nu doar cele trei: celelalte noua aveau RLS pornit (deci
citirea intorcea gol), dar tot aveau granturi catre `anon` - o politica adaugata din greseala
peste ele le-ar fi deschis dintr-o data.

---

## B2. Starea tipurilor fata de productie, pe 21.09.2026

Verificat cu `node scripts/verifica-tipuri-db.mjs`, dupa ce schema de referinta a fost
regenerata din productie.

| Ce | Stare |
|---|---|
| Tabele lipsa din tipuri | **0** (cele trei `analitice_*` au fost adaugate acum: erau in productie de pe 20.09 si lipseau din tipuri) |
| Coloane lipsa din tipuri | **0** |
| `recovery_sends` | in tipuri, **nu** in productie - migratia **12**, se aplica la final |
| `abandoned_carts.ignorat_la` | in tipuri, **nu** in productie - migratia **13**, se aplica la final |
| `store_settings` | apare ca „fantoma" fiindca in productie e **VEDERE**, nu tabela. Asta e dinadins: vederea decripteaza credentialele. Nu e nimic de facut. |

⚠ **Schema de referinta era in urma cu doua lucruri care sunt in productie**: coloanele
`recovery_optout.phone` si `.motiv` (migratia 11, aplicata pe 21.09) si functia
`aplica_praguri_oferta`. Regenerata acum, deci „schema din Git = schema din productie" e din
nou adevarat - in afara celor doua obiecte de mai sus, care asteapta ziua unirii.

---

## C. Ce se verifica dupa fiecare migratie

```sql
select p.proname,
       has_function_privilege('anon', p.oid, 'execute')          as anon_poate,
       has_function_privilege('authenticated', p.oid, 'execute') as auth_poate
  from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public'
   and p.proname in ('produse_sub_prag','numar_produse_sub_prag','stoc_combinatie',
                     'combinatie_aprinsa','fereastra_vanzari','canale_vanzare','vanzari_panou',
                     'panou_carduri');
```

Se asteapta `anon_poate = false` peste tot si `auth_poate = true`.

⚠ **`revoke ... from public` NU e de ajuns**, si s-a vazut chiar la prima migratie: privilegiile
implicite ale proiectului dau fiecarei functii noi din `public` un grant **pe nume** catre
`anon`. Ambele migratii revoca si pe nume, dar verificarea ramane: o functie noua scrisa fara
randul acela ar lasa pe oricine cu cheia publica sa ceara stocurile sau cifra de afaceri a
oricarui magazin.

---

## C2. ✅ FACUT 20.09.2026: pasul de DATE din productie

Nu e o migratie de schema, ci un `update` de o linie. **S-a facut pe 20.09.2026**, dupa push:
cele sase anunturi stinse automat s-au reaprins, deci lista de Noutati are acum cinci randuri
reale in loc de unul. Toate sase aveau `published_at` pus - adica fusesera publicate de el -
si niciunul n-avea semn de retragere anume; regula veche „unul singur odata" explica de ce
erau stinse.

⚠ Se intoarce dintr-o comanda: randurile atinse sunt in `zz_backup_anunturi_stinse_20260920`.

Pana acum, publicarea unui anunt le stingea pe toate celelalte (`unpublishOthers`),
fiindca panoul arata un singur anunt. In productie sunt **7 anunturi, dintre care
1 publicat**, desi toate sapte au data de publicare: celelalte sase au fost stinse
automat, nu retrase de el.

Cu lista noua, ele n-ar mai aparea, iar „ultimele cinci noutati" ar avea un singur rand:

```sql
-- Anunturile stinse de vechea regula „unul singur odata" se reaprind.
-- ⚠ De rulat DOAR daca el confirma ca niciunul dintre cele sase nu a fost retras dinadins.
update public.announcements set is_published = true where published_at is not null;
select count(*) filter (where is_published) as publicate from public.announcements;  -- se astepta 7
```

In baza demo s-a facut deja (plus un al cincilea rand, ca sa se vada asezarea plina).

## D2. O biblioteca noua in `package.json`

`blobatar@2.7.0` (https://github.com/Alain00/blobatar), ceruta de el pentru chipurile
utilizatorilor. N-are nicio dependenta proprie, si **nu ajunge in pachetul browserului**: SVG-ul
se deseneaza pe server, in `src/lib/avatar-blob.ts`, si se trimite ca text.

⚠ La unire trebuie sa treaca si `npm ci` pe Vercel: `package-lock.json` intra in acelasi commit
cu `package.json`.

## D. Variabile de mediu noi

Niciuna **ceruta** de cod. (Sentry a fost pus separat, in `main`, pe 18.09, cu `SENTRY_AUTH_TOKEN`
deja in Vercel.)

### ⚠ INCHIS: `TZ = Europe/Bucharest` NU SE POATE PUNE

Incercat pe 20.09.2026, dupa push. Vercel raspunde `env_key_reserved`: **`TZ` e nume
rezervat** si refuza sa fie creat, pe orice mediu. Deci propunerea nu era doar
de prisos, ci imposibila.

Nu se pierde nimic: analiza de mai devreme aratase deja ca variabila **nu e necesara**.
Codul care scrie date nu mai depinde de ceasul masinii (`formatDate`, `formatDateShort`,
`formatDateTime` si cele sase locuri din `email.ts` convertesc explicit la ora Romaniei),
iar cronurile Vercel se socotesc oricum in UTC.

⚠ Ce RAMANE de facut in locul ei: componentele de client (tabele din panou, zona de
admin) scriu inca pe ceasul VIZITATORULUI. Pentru un comerciant aflat in alt fus, orele
de acolo difera de cele de pe facturi. Se repara pe fiecare loc, nu dintr-o variabila.

---

## D3. Cele 8 chei de ramura din Vercel, dupa ID

Citite pe 20.09.2026. Se sterg **dupa id**, nu dupa nume: aceleasi nume exista si fara
ramura, pe Preview + Production, iar acelea sunt ale productiei si NU se ating.

| ID | Cheie |
|----|-------|
| `De8WIMkdZRMilZWj` | SUPABASE_SERVICE_ROLE_KEY |
| `FFSTvIfdQ8SxzDsy` | RESEND_API_KEY |
| `hvaAavTqkMXJ48rK` | NEXT_PUBLIC_SUPABASE_URL |
| `bhcSLqQSoDiKnQc5` | NEXT_PUBLIC_SUPABASE_ANON_KEY |
| `CLCp1RHrL19GceZR` | RESELLER_API_KEY |
| `3UcdKEMUf9Bs7YVt` | VERCEL_TOKEN |
| `8AI8v5yonibhRM74` | STRIPE_SECRET_KEY |
| `HuwGzKcjpUx4vO40` | SMARTBILL_TOKEN |

⚠ **Nu se sterg cat timp se mai lucreaza local**: dezvoltarea locala merge pe baza demo
prin `.env.development.local`. Sterse acum, lucrul local ar ajunge pe productie.

## D5. Un cron nou: `/api/cron/cont-curatenie`

`vercel.json` are acum **49** de cronuri, nu 48. Cel nou ruleaza la 03:51 si tine
retentia zonei de cont: sesiuni incheiate de peste 30 de zile, coduri expirate,
`cont_jurnal` peste 12 luni, contacte blocate al caror termen a trecut,
instiintari deja trimise.

⚠ Nu cere nicio variabila noua: foloseste `CRON_SECRET`, care exista deja.

⚠ Indexurile lui au fost facute in migratia de temelie, inaintea cronului. Un
index fara cititor e o promisiune goala, iar retentia promisa in sablonul de
confidentialitate al fiecarui magazin nu se tine singura.

## D4. ⚠⚠ Ramura `conturi-clienti` are nevoie de ACELEASI 8 chei, si lista de la final devine 16

Pornita pe 23.09.2026 din `main`, pentru sistemul de conturi de cumparator
([`CONTURI-CLIENTI.md`](CONTURI-CLIENTI.md)).

Masurat in aceeasi zi cu `filter_project_envs`, `gitBranch: conturi-clienti`: **lista e GOALA**.
Cele 8 chei de mai sus sunt legate de `redesign-dashboard`, si o variabila de ramura nu se
mosteneste. Deci un preview al ramurii noi cade inapoi pe valorile de Preview **fara** ramura,
care sunt **ALE PRODUCTIEI**: Supabase de productie, Resend adevarat, Stripe adevarat,
SmartBill adevarat, token Vercel adevarat.

**FACUT pe 23.09.2026.** Cele SAPTE chei de mai jos sunt legate de `gitBranch: conturi-clienti`,
toate pe `preview`. Cheia de service role a bazei demo a pus-o proprietarul.

| ID | Cheie | Valoare |
|----|-------|---------|
| `hnS50tytVVhewyZj` | NEXT_PUBLIC_SUPABASE_URL | baza demo |
| `64e74YdkJ3LeAXsA` | NEXT_PUBLIC_SUPABASE_ANON_KEY | baza demo |
| `SCXotbAR6IOY1et0` | SUPABASE_SERVICE_ROLE_KEY | baza demo |
| `eiUiJibh441bgaHx` | RESELLER_API_KEY | `dezactivat-pe-demo` |
| `yUCi2J0gFXnul2Cf` | VERCEL_TOKEN | `dezactivat-pe-demo` |
| `em1VK1wZAtNpN3jB` | STRIPE_SECRET_KEY | `dezactivat-pe-demo` |
| `HuTaUYEkkWzF9xA7` | SMARTBILL_TOKEN | `dezactivat-pe-demo` |

⚠⚠ **`RESEND_API_KEY` NU e suprascrisa pe ramura, si e o hotarare, nu o scapare.** Pusa
`dezactivat-pe-demo`, codul de intrare in cont n-ar pleca si zona de cont n-ar putea fi probata
deloc: in baza se pastreaza doar AMPRENTA codului, deci nu se poate citi de nicaieri. Nesuprascrisa,
mosteneste cheia adevarata din Preview. Cronurile NU ruleaza pe desfasurarile de preview, deci
singurele emailuri care pleaca sunt chiar codurile cerute de noi, catre adresele noastre.

⚠ **Lista de curatenie de la final creste de la 8 la 15 chei**, toate sterse DUPA ID.

⚠⚠ **`[skip ci]` NU opreste Vercel.** Incercat pe 23.09.2026: commitul gol cu `[skip ci]` in mesaj
a pornit totusi o desfasurare (`dpl_BoF9gTH2xMa39RfLExGCiydQnox6`), care a fost ANULATA de mana la
~2 minute. Verificat dupa, obiect cu obiect, ca productia n-a fost atinsa: zero tabele `privat.cont_*`,
zero coloana `cont_client_config`, iar singurele urme din acea jumatate de ora erau trafic real.
Cine mai urca o ramura noua sa stie: ordinea sigura e **urca, anuleaza pe loc desfasurarea, leaga
cheile, abia apoi declanseaza**.

---

## E. Ce s-a schimbat in cod, pe scurt

Lista e ca sa se stie **ce se uita la** dupa push, nu ca sa inlocuiasca istoricul Git
(`git log main..redesign-dashboard`).

| Zona | Fisiere | De verificat dupa push |
|------|---------|------------------------|
| Tipografie si fundal | `src/app/fonturi.ts` (nou), `src/app/stil-comun.css`, `src/app/(dashboard)/layout.tsx`, `src/app/(admin)/layout.tsx` | ⚠ Fundalul alb si Inter sunt inchise in clasa `.zona-aplicatie`. **Vitrinele raman pe Geist si pe fundalul lor**: se verifica o vitrina reala, nu doar panoul. |
| Suprafete | `src/components/ui/panel.tsx` | Cardurile au `ring-1 ring-foreground/10`, nu chenar. |
| Stoc scazut | `src/lib/stoc-prag.ts`, `src/lib/actions/stoc-scazut.actions.ts`, `src/components/dashboard/StocScazutRand.tsx`, `src/components/dashboard/ModalStocScazut.tsx`, pagina panoului | Banda arata numarul corect; modalul deschide lista; „Ignora" tine intre reincarcari. Depinde de migratia **1**. |
| Grafic de vanzari | `src/lib/vanzari.ts` (+ probe), `src/components/dashboard/PanouVanzari.tsx`, `src/components/dashboard/GraficVanzari.tsx`, pagina panoului; **sters**: `src/components/dashboard/RevenueChart.tsx` | Perioadele, canalele si comparatia. Depinde de migratia **2**. |
| Cele patru carduri | `src/lib/panou-carduri.ts` (+ probe), `src/components/dashboard/ExplicatieCard.tsx`, pagina panoului | Comenzi azi, Vanzari luna aceasta, Valoare medie comanda, Rata de conversie, fiecare cu diferenta procentuala; tooltip cu formula doar la conversie. Depinde de migratia **3**. ⚠ Se verifica si pagina **Statistici**: vizitele de acolo trebuie sa creasca dupa aplicarea politicii. |
| Etichete de stare | `src/components/ui/eticheta-stare.tsx`, `src/lib/orders/status.ts`, `OrdersClient`, `OrderDetailClient`, `CustomersClient`, `ReturnsClient`, `EmagListings`, `EmagJurnal`, `EmagProbleme`, `TrendyolListings`, `TrendyolAutoMap`, `TrendyolClient`, `AboutYouListings`, `AboutYouOrders`, `AboutYouPreVerificare`, `AboutYouCategoryMapping`, `GoogleMerchantClient`, `OlxClient`, `StatisticiVanzari`, pagina panoului si `dashboard/features` | Un singur fel de eticheta in TOT panoul (22 de fisiere): punct colorat pe fundal neutru, varianta aleasa de el pe 20.09.2026. Se verifica: Comenzi, Retururi, Integrari (cartonasele), eMAG „Ce te tine pe loc”, listarile de marketplace. ⚠ La Google Merchant si OLX a iesit si ICONITA din eticheta: punctul spune acelasi lucru, iar douazeci de cartonase cu pastile pline faceau pagina sa tipe. ⚠ **RAMAN pe fundal plin, si asa trebuie**: cele trei butoane de schimbare a starii din `OrderDetailClient` sunt COMENZI, nu etichete - optiunea aleasa trebuie sa se vada apasata. |
| Comenzi recente | pagina panoului, `src/lib/utils/format.ts` (`acumCatTimp`) | Fiecare rand spune cand a venit comanda („acum 12 minute") si de unde (eMAG, Google Ads, Direct). |
| Fusul orar | `src/lib/utils/format.ts`, `src/lib/email.ts` (+ probe care pornesc un Node pe `TZ=UTC`) | Datele scrise de server erau pe UTC: o comanda de la 01:30 aparea „19 septembrie, 22:30". Acum toate trec prin ceasul romanesc. ⚠ RAMAS: componentele de client (tabele din panou, zona de admin) scriu pe ceasul VIZITATORULUI; pentru un comerciant din alt fus, orele difera de facturi. De facut in aceeasi trecere cu etichetele. |
| Noutati | `src/components/dashboard/ListaNoutati.tsx` (nou), `AnnouncementArticle`, `src/lib/announcements.ts` (`rezumatScurt`), `src/lib/actions/announcement.actions.ts`, pagina panoului | Cinci randuri in loc de un articol desfasurat, fara iconita la titlu. ⚠ **Publicarea nu mai stinge celelalte anunturi** (`unpublishOthers`, scoasa): altfel lista ar fi avut mereu un singur rand. Vezi pasul de date de mai jos. |
| Alertele de cont | `src/components/dashboard/BandaCont.tsx` (nou), `TrialBanner`, `PaymentPastDueBanner`, `GracePeriodBanner`, `ActivationChecklist`, `src/lib/abonament-timp.ts` (nou, + probe), `src/app/(dashboard)/layout.tsx` | O singura forma pentru toate vestile despre abonament, cu trei trepte (informare, atentie, urgent). ⚠ Zilele ramase se socotesc acum pe SERVER si se trimit ca prop: `GracePeriodBanner` e componenta de client si citea ceasul in randare, deci serverul si browserul puteau ajunge la doua numere diferite. |
| Navigatie | `src/lib/navigatie-panou.ts` (nou), `Sidebar`, `DashboardTopbar`, `BottomNav`, probele din `navigatia-nu-divergeaza.test.ts` si `decontarile-se-aduna-zi-cu-zi.test.ts` | Meniul e acum unul singur: erau doua copii, iar de pe telefon lipseau Oferte, SMS Marketing si Design sectiuni. Ordine noua, sageti la sectiunile cu submeniu, fara eticheta Beta la Oferte. Bara de jos: Acasa / Comenzi / Produse / Statistici. |
| Bara de sus | `CautareGlobala`, `ButonAdauga` (noi), `src/lib/actions/cautare-globala.actions.ts`, `src/lib/cautare-termen.ts`, `src/lib/avatar-blob.ts`, `DiscountsClient` | Cautare in produse, comenzi si clienti, cu rezultate sub camp. Buton Adauga (produs, discount, oferta). Chip de utilizator desenat din id. Doar prenumele. Facturare si abonament in meniul contului. |
| Sigla si starea magazinului | `src/lib/stare-magazin.ts` (nou, + probe), `Sidebar` | Sigla in locul initialei; bulina: verde publicat si domeniu bun, galben nepublicat, rosu domeniu cazut. |
| **Conturi de client, Etapa A** | `src/lib/cont/**` (nou: `origine`, `jeton`, `sesiune`, `cod`, `magazinul-cererii`, `pagina`, + probe), `src/app/api/cont/{cod,intra,iesire}/route.ts` (noi), `src/app/(public)/[slug]/cont/**` (nou), `src/components/storefront/cont/FormularIntrare.tsx` (nou), `src/lib/pages/reserved-slugs.ts`, `src/lib/email.ts` (`sendCodCont`), `src/types/database.types.ts` | ⚠ Depinde de migratiile **38-41**. Se verifica pe un magazin cu DOMENIU PROPRIU si cu `cont_client_config.enabled = true`: `/cont/intra` cere emailul, codul ajunge cu marca magazinului, `/cont` deschide contul. ⚠ Pe `www.edinio.com/<slug>/cont` trebuie **404** (originea comuna nu poate apara un cont). ⚠ Stinsa setarea: 404 pe rute, dar iesirea din cont trebuie sa mearga mai departe. ⚠ Cumparatorul anonim nu plateste nimic in plus: niciun cookie, nicio cerere noua. |
| **Conturi de client, setarea din panou** | `src/lib/actions/cont-client.actions.ts` (nou), `src/components/dashboard/setari/ContClientiSetari.tsx` (nou), fila noua in Setari, `src/lib/cont/origine.ts` | ⚠ Depinde de migratiile **42** si **46**. Comutatorul se aprinde DOAR pe un magazin cu domeniu propriu sanatos, iar motivul se scrie langa el, nu dupa apasare. ⚠⚠ Refuzul e si in actiunea de server, nu doar in ecran: fiecare export dintr-un modul `"use server"` e un endpoint public. ⚠ Dupa salvare se RECITESTE si se compara: `jsonb_merge_config` iese tacut cand magazinul n-are inca rand in `store_settings`, si actiunea ar fi raspuns „salvat" fara sa scrie nimic. ⚠ Numarul de conturi vine prin `cont_cate_conturi`, fiindca tabelele stau in `privat`, pe care PostgREST nu o expune. |
| **Conturi de client, ecranele** | `src/app/(public)/[slug]/cont/{comenzi,comenzi/[id],date,retururi,preferinte}` (noi), `src/app/api/cont/{atinge,preferinte,contact,comanda,export,sterge}/route.ts` si `src/app/api/cont/factura/[orderId]/route.ts` (noi), `src/lib/cont/{comenzi,date,preferinte,cerere}.ts` (noi, + probe), `src/components/storefront/cont/**` (noi), `src/app/(public)/[slug]/layout.tsx` | ⚠ Depinde de migratiile **43**-**45**. Comenzile, factura, datele de contact, retururile, preferintele de marketing, exportul si stergerea contului. ⚠⚠ Comenzile de pe marketplace NU se leaga niciodata de un cont (`not coalesce(o.order_source ? 'marketplace', false)`): cumparatorul de pe eMAG n-a cumparat de la magazin, iar telefonul lui ar fi fost cheia catre comenzi straine. ⚠ Pixelii vitrinei sunt inveliti in `<DoarInAfaraContului>`: zona de cont nu are ce sa raporteze nimanui. ⚠ Exportul pleaca prin POST, nu printr-un `<a>`: o adresa GET cu date personale ajunge in jurnale si in istoricul browserului. |
| **Conturi de client, curatenia** | `src/app/api/cron/cont-curatenie/route.ts` (nou), `vercel.json` (48 → 49 cronuri) | ⚠ Depinde de migratia **45**. Sesiuni incheiate de peste 30 de zile, coduri expirate de peste o zi, `cont_jurnal` mai vechi de 12 luni (poarta IP-uri), contacte blocate cu termenul trecut, instiintari trimise mai vechi de 30 de zile. ⚠ Exista fiindca indexurile `*_curatenie` fusesera facute pentru un cron care nu era scris, iar retentia promisa in politica FIECARUI magazin nu se tine singura. ⚠ Conturile sterse NU se ating: raman doi ani, golite de date, ca sa se poata raspunde la „de ce nu mai pot intra". |
| ⚠ Logoul din email, adrese pe care clientul le blocheaza | `src/lib/email/logo-email.ts`, `src/lib/email/logo-email.test.ts` | Gasit **pe bune**, la primul email de intrare in cont trimis catre el pe 23.09.2026: mesajul a venit cu marca magazinului, dar in locul logoului n-a aparut nimic. Magazinul demo are logoul ca SVG inline (`data:image/svg+xml;base64,…`), iar `new URL("data:…")` REUSESTE, deci adresa trecea prin toate gardurile si ajungea neatinsa in `<img src>`; clientii de email blocheaza `data:`. Acum `logoPentruEmail` intoarce `null` pentru orice adresa care nu e http(s) si pentru `.svg`, iar invelisul scrie NUMELE magazinului, drumul pe care il fac oricum 42 din cele 71 de magazine publicate. ⚠ **MASURAT PE PRODUCTIE inainte de atins: ZERO magazine au logo `data:` sau `.svg`** (42 fara logo, 29 `.webp`), deci schimbarea nu muta nimic pentru niciun comerciant de azi, doar inchide drumul. ⚠ Proba cere si ca adresele bune sa treaca mai departe: o plasa prea larga ar fi stins logoul celor 29 care il au. |
| **Conturi de client, banii comenzii** | `src/lib/cont/banii-comenzii.ts` (nou, + proba), `src/lib/cont/comenzi.ts`, `src/lib/cont/pagina.ts` (`setariTva`), `src/app/(public)/[slug]/cont/comenzi/[id]/page.tsx`, `src/lib/cont/regulile-contului.test.ts`, `src/types/database.types.ts` | ⚠ Depinde de migratia **47**. Randurile de bani ale comenzii din cont vin acum din `randuriDeBani`, CHIAR functia emailului de confirmare, care cheama `totaluriComanda`, modulul casetei din panou: trei suprafete, aceleasi randuri. ⚠⚠ Regimul INGHETAT bate setarea de azi, ca in `invoiceVat`; `totaluriComanda` ii da lui `invoiceVat` numai cota, deci regimul ajunge prin setare. ⚠ Vederea redusa nu primeste defalcare: „Produse” plus „Transport, taxe si reduceri”. ⚠⚠ Proba vederii reduse citea definitia din migratia 43, deci ar fi ramas verde pe una veche: acum citeste ULTIMA, are lista definitiilor in ordinea aplicarii (numele fisierelor NU o dau) si cade la o definitie necunoscuta; cere si ca FIECARE aparitie a unui camp de bani sa fie imbracata, nu macar una. Probe: 10 noi; 5 mutanti pe functie si 4 pe migratie, toti prinsi, pe o baza verde. **Se verifica dupa push**: pe o comanda platita cu cardul, `/cont/comenzi/<id>` arata „Reducere plata cu cardul”, NU „Alte ajustari”. |
| **Conturi de client, redesign si comanda completa** | NOI: `src/components/storefront/cont/ui/**` (`clase`, `piese`, `meniu`, `MeniuFile`, `CadruCont`, `PaginaCont`), `src/components/storefront/cont/ecrane/**` (Acasa, Comenzi, Comanda, Facturi, Retururi, Date, Preferinte, Intrare, `BlocDocument`, `CardComanda`, `Paginare`), `DescarcaDocument.tsx`, `CopiazaText.tsx`, `src/app/(public)/[slug]/cont/facturi/page.tsx`, `src/app/api/cont/factura/[orderId]/storno/route.ts`, `src/app/api/cont/iesire-peste-tot/route.ts`, `src/lib/cont/{urmarire,plata,cronologie,livrare,detalii-checkout,documente,facturi,rezumat,pdf-document,ruta-document}.ts` (+ doua fisiere de probe). RESCRISE: toate paginile de cont, cele cinci componente de client, `comenzi.ts`, `date.ts`, `pagina.ts`, ruta facturii. Atinse in afara contului: `src/lib/smartbill.ts` (semnal optional la `fetchMerchantPdf`), `src/lib/billing/factura-comenzii.ts` (`GAZDE_DE_TEST` exportat), `src/lib/storefront/design/css-vars.ts` (token nou `--st-on-bg`), `database.types.ts` | ⚠ Depinde de migratia **48**. Cerut de el: design premium, meniu, comanda completa, factura vazuta si descarcata. ⚠⚠ Zona de cont e acum pe TOKENII MAGAZINULUI (`--st-*`), nu pe ai panoului si nu pe `businesses.primary_color`: pe Casa Lumen iesea verdele Edinio in loc de teracota; butoanele primare folosesc `--st-primary-contrast` (albul pe primara e sub 4,5:1 la 55 din 71 de magazine). Proba noua interzice in zona de cont `text-foreground`, `text-white`, `p.color` si ceilalti. ⚠ `--st-on-bg`: textul scris DIRECT pe fundalul magazinului; ramane `var(--st-text)` cand contrastul trece, deci la magazinele obisnuite nu schimba nimic (un magazin de pe productie are fundalul #5b2067, unde textul temei iesea la 1,56:1). Il citeste deocamdata numai contul. ⚠⚠ Factura: descarcarea trece prin `fetch`, iar esecul se spune IN card, cu motivul (niciodata „nu exista factura”); documentul de test se opreste INAINTE de orice cerere; limita de timp 10 s; plafon de 4,4 MB; fiecare esec se scrie in `error_logs` (inainte tacea); stornarea se descarca pe ruta ei. ⚠ Linia de timp nu inventeaza ore: numai plasarea si AWB-ul au data (nu exista istoric al starilor). Urmarirea coletului foloseste NUMAI tiparele deja folosite in panou (FAN, Cargus, Sameday, DPD) sau adresa salvata de curier, si aceea numai `https:`. „Iesi de pe toate dispozitivele” cheama `cont_iesi_de_peste_tot`, care exista dar nu era chemata de nimeni. ⚠ Demo: `demo-seed/90-cont-proba-facturi.sql` leaga de contul de proba sase comenzi Casa Lumen cu facturi si retururi (SmartBill emisa si stornata, Oblio, fGO, fGO de test). Descarcarea PDF pe demo NU merge (token SmartBill fals, linkuri semanate): acolo se vede mesajul cinstit de esec. Probe: 9863 verzi; lint 53 (neurcat). **Se verifica dupa push**: meniul (lateral pe desktop, file pe telefon) cu numerele; pe o comanda cu factura, „Descarca factura” aduce PDF-ul; pe una stornata apare nota de stornare; codul postal apare in adresa; ridicarea personala nu apare ca livrare la adresa. |
| **Conturi de client, setarile, butonul din antet si contul obligatoriu** | NOI: `src/lib/cont/config.ts` (setarea curatata la fiecare citire, pura), `src/lib/cont/poarta-comenzii.ts` (poarta de la comanda si legarea la plasare), `src/app/api/cont/stare/route.ts` (GET: ce stie formularul), `src/components/storefront/cont/{ActiuneCont,ContulMagazinului,iconite-cont,ContulDupaComanda,contul-la-comanda,intrare-cu-cod}`, `src/components/storefront/sections/checkout/IntrareLaComanda.tsx`, `src/lib/cont/setarile-contului.test.ts`. RESCRISE: `ContClientiSetari.tsx`, `cont-client.actions.ts`, `FormularIntrare.tsx` (pe `useIntrareCuCod`), `urmarire.ts`. ATINSE: cele 8 antete din `sections/chrome/` si sertarul din `StoreNav.tsx`, `[slug]/layout.tsx`, `antet-magazin.ts`, `order.actions.ts` (poarta in `placeOrder` si `placeCartOrder`, legarea dupa insert, `cont_url` in emailul de confirmare), `checkout-core.ts`, `CheckoutForm.tsx`, `OrderModal.tsx`, `confirm/page.tsx`, `email.ts`, `api/cont/contact` (legarea dupa confirmarea unui contact nou), `pagina.ts` + `EcranIntrare.tsx` (textele din Setari), `EcranComanda.tsx`, `comenzi.ts`, `origine.ts` (`adresaContului`). In afara contului: `totals-box.ts` (+ `regimInghetat`), `OrderDetailClient.tsx`, `order-totals.ts`, `curierul-declarat.test.ts` (proba pe subsir reparata) | ⚠ Depinde de migratia **49** (si de toate 38-48). Cerut de el pe 23.09.2026, SCHIMBA H1: comerciantul alege din Setari cont OPTIONAL sau OBLIGATORIU la comanda; butonul „Contul meu” din antet e reglabil (iconita, text sau amandoua; 8 iconite; textul lui); pagina de intrare are titlul, textul si avantajele din Setari; nimic nu porneste fara domeniu propriu sanatos (scris in Setari, cu drum spre setarea domeniului). ⚠⚠ Poarta contului e PE SERVER, in ambele actiuni, INAINTEA oricarei scrieri (numar, cupon, stoc, insert); proba o cere. Cade DESCHIS, cu avertisment, cand domeniul e masurat cazut (vitrina merge atunci pe www.edinio.com) si cand plafonul zilnic de coduri e epuizat. ⚠ Formularul afla de la deschidere (`/api/cont/stare`, numai la magazinele cu cont obligatoriu, din ACEEASI poarta) daca trebuie sa intri, iar pasul de intrare sta IN AFARA `<form>`-ului comenzii; trimiterea se opreste inaintea pixelilor (`AddPaymentInfo`). Cosul, campurile si cuponul raman pe loc; emailul verificat intra in campul de email numai cand acela se vede si e gol. ⚠ Contul obligatoriu se poate porni numai dupa o intrare reala in ultimele 30 de zile si cu plafon de coduri de minim 50. ⚠⚠ `suspended_until` e termen de GRATIE (magazinul se opreste cand data a TRECUT): prima scriere a butonului il citea invers; reparat, cu proba. ⚠ Urmarirea coletului: Woot duce la pagina curierului REAL (DPD la 273 din 274), plus GLS, Packeta, eColet, Shipo, Innoship; Posta si Pall-Ex au doar pagina lor de cautare, deci apar ca legatura, nu ca buton. ⚠ In afara contului: pagina comenzii din panou eticheta TVA-ul dupa setarea de AZI a magazinului, nu dupa regimul inghetat pe comanda; `totaluriComanda` cere acum `regimInghetat` (obligatoriu, ca orice apelant nou sa fie numarat de compilator). **Se verifica dupa push**, pe `edn.inovex.ro`: in Setari, butonul si iconita se vad in previzualizare si in antet; cu contul obligatoriu, `/checkout` arata pasul de intrare, codul vine pe email, dupa intrare comanda pleaca si apare in `/cont/comenzi`; pagina de confirmare arata cardul spre cont. |
| **Conturi de client, panoul comerciantului (Clienti > Conturi)** | `src/lib/cont/{panou,panou-texte}.ts` (noi), `src/lib/actions/conturi-panou.actions.ts` (nou), `src/components/dashboard/clienti/conturi/*` (noi), `src/app/(dashboard)/dashboard/customers/conturi/[id]/page.tsx` (nou), `src/app/(dashboard)/dashboard/customers/page.tsx`, `CustomersClient.tsx`, `clienti/{FilelePaginii,FilaSegmente}.tsx`, `src/lib/customers/{segmente,filtre}.ts`, `setari/ContClientiSetari.tsx`, `src/lib/cont/autentificare.ts`, `src/types/database.types.ts`, probe in `src/lib/cont/panoul-conturilor.test.ts` (24) si `segmentul-are-un-singur-loc.test.ts` | Fila noua **Conturi** in Clienti: cifre (conturi, intrate in 30 de zile, cu comenzi, suspendate), lista cu cautare (nume, email, telefon, numar de comanda), filtre si ordine in adresa. **Fisa contului**: contacte, comenzile din cont (cu temeiul), istoric (ultimele 100, IP ascuns), securitate (parola, sesiuni, dispozitive, parole gresite 24 h), preferinte. **Actiuni**, fiecare cu confirmare: iesire de pe toate dispozitivele, suspendare cu motiv / reactivare, stergere la cererea omului, legare de mana in doi pasi (se ARATA comanda si daca emailul/telefonul ei e al contului, abia apoi se leaga), dezlegare numai pentru ce a legat comerciantul. **In vitrina**: contul suspendat afla abia dupa parola buna (sau dupa cod) ca e suspendat, fara cod trimis si fara sesiune. **In lista de clienti**: eticheta „Cont”, legatura din fisa, filtrul „Numai cu cont în magazin” (prin `p_chei`, intersectat cu segmentul), salvat si in segmente (`CriteriiSegment.cont`). **Reparat pe drum**: placa unui segment salvat cu judet sau canal arata numarul intregului segment (`numaiSegmentul`). **Recenzie independenta (securitate+SQL, ecrane+logica)**, reparat: salvarea segmentului NU scria criteriul `cont` (segmentul „cu cont” se deschidea fara filtru; proba noua cere ca fiecare criteriu sa ajunga in scriere); inapoi/inainte in cautarea conturilor; filele Clientilor la 360px; confirmarea „Sigur?” care ramanea armata in Safari; raspunsul unei cautari vechi la legare. Ramas stiut: daca suspendarea cade exact intre verificare si deschiderea sesiunii, omul vede o eroare generica (sesiunea tot nu se deschide). ⚠ Pe demo, contul „Proba Oferte” are comenzile a 12 clienti demo (semanate asa pentru facturi, cu emailul contului), deci eticheta apare si la ei. |
| **Conturi de client, intrebarile de dinaintea unirii (font, paginare, „Datele mele”)** | `src/lib/cont/{paginare.ts (nou),comenzi,facturi,autentificare,cod,cerere-stergere}.ts`, `src/app/(public)/[slug]/cont/{comenzi,facturi,date}/page.tsx`, `src/app/api/cont/{sterge,contact,parola,iesire-peste-tot,export,comanda}/route.ts`, `src/components/storefront/cont/{StergeContul.tsx,ecrane/{EcranDate,EcranIntrare,Paginare}.tsx,ui/{CadruCont.tsx,clase.ts}}`, `src/lib/email.ts`, probe noi in `src/lib/cont/datele-mele.test.ts` (18) | Intrebat de el inainte de unire: fontul, paginarea la „Comenzile mele” si daca merg functiile din „Datele mele”. **Fontul**: zona de cont ia fontul titlurilor si al textului din Design (`--st-font-heading`/`--st-font-body`); nou: `fontSynthesisWeight: "none"` pe invelis, fiindca Instrument Serif are o singura greutate si titlurile mici ieseau ingrosate artificial. **Paginarea**: Comenzi si Facturi, cate 20; `?p=` stricat sau urias nu mai da eroare de baza (plafon 100 000, decalaj plafonat si in functie), o pagina dupa capat trimite la prima; butoanele unite prin `cn` (clasele lipite ca sir se bateau), pe telefon numai sageti. Comanda cu id care nu e uuid: 404, nu eroare. Retururile NU au paginare (lista intreaga, fara plafon). **Datele mele**: iesirea de peste tot uita si dispozitivele (migratia 52), iar la eroare te lasa in cont si spune; exportul da toate comenzile cu detaliu (52), iar la eroare te intoarce cu mesaj in loc de o pagina alba; textul despre sesiuni spune limitele adevarate (30 de zile, 14 fara folosire); stergerea contului cere PAROLA; parola din cont se verifica intr-un singur loc (`parolaDinCont`), cu plafoanele intrarii, la schimbarea parolei, la adresa noua si la stergere; adresa noua: forma verificata inainte, 429 pe plafonul de IP, text cinstit („Daca adresa poate fi adaugata...”); plafoanele codurilor pe IPv6 socotite pe /64. Emailul cererii de stergere spune „si inca N” peste 100 de comenzi. | 
| **Conturi de client, verificarea din 24.09.2026 si reparatiile ei** | `src/lib/cont/{autentificare,cerere,poarta-comenzii,cerere-stergere,urmarire}.ts`, `src/lib/storefront/antet-magazin.ts`, `src/app/api/cont/{inregistrare,pas,parola,contact}/route.ts`, `src/components/storefront/cont/{autentificare-client.ts,contul-la-comanda.ts,FormularIntrare,GestioneazaContacte,StergeContul,ContulDupaComanda}.tsx`, `ecrane/{EcranIntrare,EcranDate}.tsx`, `sections/checkout/IntrareLaComanda.tsx`, `src/lib/email.ts`, `src/lib/customers/anonimizare.ts`, cronul `cont-curatenie`, probe noi in `autentificarea.test.ts`, `setarile-contului.test.ts`, `anonimizarea-nu-sterge-dezabonarea.test.ts` | Cerut de el: „dupa ce finalizezi tot mai poti face o verificare”. Patru recenzenti independenti (siguranta intrarii, ecranele, stergerea datelor si SQL-ul, regresii pe drumul comenzii). Reparat tot ce au gasit, cu doua exceptii scrise mai jos. **Siguranta:** emailurile de cont nou si resetare pleaca DUPA raspuns (`after`), deci nici timpul nu mai spune cine are cont; cont nou pe o adresa cu cont primeste alt email („codul SETEAZA O PAROLA NOUA”); plafonul pe IP inaintea lui scrypt; IPv6 socotit pe retea (/64); o adresa noua in cont cere parola contului; parola schimbata din cont pastreaza „dispozitivul de incredere” numai daca era. **Comanda:** un magazin cu cont obligatoriu si domeniul deconectat nu mai opreste vanzarile (merge ca vizitator, cu avertisment); refuzurile poartei se scriu in jurnal; contul optional fara cookie nu mai face nicio cerere; antetul vitrinei nu mai devine 404 la toate magazinele daca pica citirea (jurnal + citire de rezerva fara coloana conturilor). **Ecrane:** un raspuns venit dupa schimbarea filei se arunca (inainte, un cod de intrare ateriza pe ecranul de resetare si omul credea ca a schimbat parola); campurile se opresc cat asteapta; pasul codului arata adresa pe care a plecat codul; un cod lipit „123 456” nu mai pierde o cifra; focusul merge pe camp; numaratoarea nu mai e citita in fiecare secunda; „Tine minte” pleaca nebifat; termenii si la contul facut din comanda; dupa o cerere de stergere esuata se arata emailul si telefonul magazinului. **Exceptii, dinadins:** datele din etichetele de curier deja generate si din datele brute primite de la marketplace-uri raman (scris cinstit in textele catre comerciant); evenimentele catre pixeli pot pleca de doua ori pe drumul rar in care `/api/cont/stare` cade (compromis stiut). Probe: 9911 verzi; lint neurcat; build:ci ok. |
| **Conturi de client, email si parola; stergerea datelor** | NOI: `src/lib/cont/{parola,parola-reguli,autentificare,cerere-stergere}.ts`, `src/app/api/cont/{inregistrare,pas,parola}/route.ts`, `src/components/storefront/cont/{autentificare-client.ts,SchimbaParola.tsx}`, `src/lib/cont/autentificarea.test.ts`. RESCRISE: `api/cont/intra/route.ts` (email + parola), `FormularIntrare.tsx`, `IntrareLaComanda.tsx`. STERSE: `api/cont/cod/route.ts` (intrarea numai cu cod), `intrare-cu-cod.ts`. ATINSE: `cod.ts` (numai `adaugare-contact`), `cerere.ts` (`ipPentruBaza`), `config.ts` (`verificare_intrare`), `ContClientiSetari.tsx`, `EcranIntrare.tsx`, `EcranDate.tsx`, `StergeContul.tsx`, `ContulDupaComanda.tsx`, `api/cont/sterge`, `cont/intra` si `cont/date` (pagini), `email.ts` (`sendCodCont` pe scopuri, `sendParolaSchimbata`, `sendCerereDeStergere`, textul din confirmarea comenzii), `magazinul-cererii.ts`, `date.ts`, cronul `cont-curatenie`, `database.types.ts`. In afara contului: `customers/anonimizare.ts` (textele din confirmarea anonimizarii), `anonimizarea-nu-sterge-dezabonarea.test.ts` (citeste ULTIMA definitie + 3 probe noi) | ⚠ Depinde de migratiile **50** si **51**. Cerut de el pe 24.09.2026. Intrarea: email + parola; pe un dispozitiv necunoscut se cere si un cod pe email (sau la fiecare intrare, daca asa alege comerciantul in Setari), cu „Tine minte acest dispozitiv 60 de zile”; contul nou se creeaza abia dupa codul de pe email; „Ai uitat parola?” = cod + parola noua; parola se schimba si din cont (Datele mele). ⚠⚠ Fara oracol: contul nou si resetarea raspund la fel pentru orice adresa, cookie-ul provocarii se pune inaintea oricarei iesiri, iar la intrare adresa fara cont si contul blocat consuma aceeasi munca (`verificareOarba`) si primesc acelasi text ca o parola gresita. ⚠ Dupa orice schimbare de parola, celelalte dispozitive ies din cont si omul primeste emailul „parola s-a schimbat”. ⚠ Reparat pe drum: `clientIp` intoarce `unknown` cand lipseste antetul, iar contul compara cu `necunoscut`; sirul ar fi ajuns intr-un parametru `inet` si ar fi rupt sesiunea (pe Vercel antetul exista mereu, deci nu s-a vazut). ⚠ Stergerea contului poate trimite comerciantului o CERERE de anonimizare a comenzilor (bifa, email cu contactele si termenul de o luna); anonimizarea nu se face automat, comenzile sunt ale lui. ⚠ Colete Online ramane fara adresa de urmarire: zero AWB emise vreodata si nicio adresa verificata. Probe: 9898 verzi; lint neurcat; build:ci ok. **Se verifica dupa push**, pe `edn.inovex.ro`: cont nou (codul vine pe email), iesire, intrare cu parola pe acelasi browser (fara cod, daca s-a bifat), intrare dintr-o fereastra privata (cere codul), „Ai uitat parola?”, schimbarea parolei din Datele mele, contul obligatoriu la `/checkout`. |
| Tipuri | `src/types/database.types.ts` | ⚠ Intrarile pentru functiile noi sunt **scrise de mana** (regenerarea completa rescrie `store_settings` din tabela in vedere si rupe zeci de locuri). Dupa aplicarea migratiilor, ele descriu in sfarsit ceva ce exista si in productie. |
| Cosuri abandonate | `src/lib/abandoned/suprimare.ts` si `src/lib/abandoned/sms-segmente.ts` (noi, + probe), `src/lib/actions/abandoned-cart.actions.ts`, `src/app/api/cron/abandoned-recovery/route.ts`, `src/components/dashboard/AbandonedCartsClient.tsx`, `src/components/dashboard/cosuri/` (nou) | Trimiterea de MANA respecta acum dezabonarea (pe email SI pe telefon, normalizat) si refuza cosurile deja finalizate - pana acum numai cronul verifica, panoul nu atingea lista. Se verifica: un rand in `recovery_optout` opreste si emailul, si SMS-ul, din panou. ⚠ Socoteala SMS de sub casuta arata acum ce se plateste: masurat pe un mesaj romanesc adevarat, vechiul rand scria „1 SMS" pentru ceva ce pleaca in **3**. Depinde de migratiile **11**-**16**. |
| Clienti (etapele A-F) | `src/lib/customers/` (`bani.ts`, `etichete.ts`, `filtre.ts`, `segmente.ts`, noi, + probe), `src/lib/perioade.ts` (mutat din `lib/abandoned/`), `src/lib/actions/customer-segments.actions.ts` (nou), `src/lib/actions/customer.actions.ts`, `src/lib/actions/customer-import.actions.ts`, `src/components/dashboard/CustomersClient.tsx`, `src/components/dashboard/clienti/` (nou), `src/app/(dashboard)/dashboard/customers/page.tsx`, `src/lib/utils/format.ts` | Pagina spune acum ce masoara: „Valoarea comenzilor" si „Total incasat" in locul lui „Total cheltuit", care promitea bani si numara altceva. Badge-ul „Fidel" a devenit „Recurent" (aparea la a DOUA comanda). Lista are tabel pe desktop, filtre socotite in baza, si sertar lateral cu patru file in loc de fereastra. Pagina are trei file: Toti clientii · Segmente · Importuri. ⚠ Butonul de EXPORT a fost scos, la cererea lui directa. ⚠ `formatPhoneDisplay` scria `+4 0755 588 107`; reparat - avea ZERO apelanti inainte, deci nimeni n-o vazuse. ⚠⚠ Importul lasa acum o urma (cand, ce fisier, cati au intrat): pana azi nu lasa nimic. ⚠ **F3 (legarea segmentelor de campanii) NU e facuta**, si nu din lipsa de timp: campaniile SMS au alt vocabular de filtre, iar o potrivire pe jumatate ar trimite o campanie platita altei liste de oameni. Isi are locul la G5, cu consimtamantul si dezabonarea. Depinde de migratiile **17**-**22**. |
| Clienti (etapa G, partial) | `src/lib/customers/gestionare.ts` (nou, + proba), `src/lib/actions/customer-manage.actions.ts` (nou), `src/components/dashboard/clienti/AdaugaClient.tsx` si `StergeContact.tsx` (noi), `src/lib/customers/etichete.ts`, `src/lib/customers.ts`, `src/lib/customers/filtre.ts` | Se poate adauga un client de mana (comenzi telefonice, magazin fizic, lead-uri) si se poate sterge un contact FARA comenzi. ⚠⚠ Un cumparator NU se sterge niciodata: are in spate facturi plecate la SmartBill si la client, iar sters ar lasa comenzi fara nume. Paza e in chiar instructiunea de stergere. ⚠⚠ Adaugarea de mana a facut badge-ul „Importat" sa minta in aceeasi zi: acum se citeste `customers.source`, si eticheta s-a despartit in „Importat" / „Adaugat manual". ⚠ **G2 (arhivare), G3 (anonimizare) si G5 (actiuni rapide) NU sunt facute** — vezi `docs/redesign/CLIENTI.md` pentru ce lipseste la fiecare. Depinde de migratiile **23**-**24**. |
| Clienti (C2 final + D4) | `src/lib/customers/filtre.ts`, `src/lib/customers/segmente.ts`, `src/lib/customers.ts`, `src/components/dashboard/CustomersClient.tsx`, `src/app/(dashboard)/dashboard/customers/page.tsx` | Filtre pe **judet** si pe **canal**, cu meniuri facute din ce exista chiar in magazin si cu numarul langa fiecare optiune („Cluj (18)"). Meniul nu apare daca magazinul are o singura valoare. ⚠ Cele doua intra si in criteriile segmentelor salvate, odata cu filtrele: adaugate dupa, un segment salvat intre timp ar fi pastrat jumatate de filtru. ⚠ **D4 era deja rezolvata de D1**: masurat, pozitia de derulare ramane 800px inainte, in timpul si dupa deschiderea fisei, fiindca sertarul lateral nu navigheaza nicaieri. Depinde de migratia **25**. |
| Clienti (anonimizare + selectie in masa + telefon) | `src/lib/customers/anonimizare.ts`, `selectie.ts`, `src/lib/csv.ts` (noi, + probe), `src/lib/abandoned/lista.ts`, `src/lib/actions/customer-manage.actions.ts`, `src/lib/actions/customer-segments.actions.ts`, `src/components/dashboard/clienti/BaraSelectie.tsx` (nou), `StergeContact.tsx`, `SegmenteSalvate.tsx`, `FilaSegmente.tsx`, `AdaugaClient.tsx`, `src/components/dashboard/CustomersClient.tsx`, `src/components/dashboard/CardStatistica.tsx`, `src/app/(dashboard)/dashboard/customers/page.tsx` | Cerute de el pe 21.09.2026, dupa prima livrare. **Anonimizarea** unui client (a ales-o in locul stergerii, intrebat) si **selectia in masa** cu trei actiuni: sterge/anonimizeaza, fa un segment cu lista, descarca cei bifati. ⚠⚠ A scos la iveala o **gaura de securitate in cod deja livrat**: exportul de cosuri abandonate nu oprea formulele Excel — un nume scris `=HYPERLINK(...)` la checkout se EXECUTA la deschiderea fisierului. Regulile de CSV s-au mutat in `lib/csv.ts` si apara acum amandoua exporturile. ⚠ Si trei reparatii de asezare pe telefon, cerute de el: cardurile treceau pe doua coloane si rupeau cifrele (masurat: „407,11 lei" cere 165px, cardul lasa 129), bara de sus se strangea, filtrele ieseau de lungimi deosebite. ⚠ Gasit tot atunci si reparat: in lista, etichetele impingeau NUMELE afara (`Radu Ene` se scria `Radu ...`). Depinde de migratiile **26**-**27**. |
| Discounturi (etapele A-E) | `src/lib/discounts/` (`stare.ts`, `filtre.ts`, `mesaj.ts`, `perioada.ts`, noi, + 5 fisiere de probe), `src/lib/actions/discount.actions.ts`, `src/lib/actions/order.actions.ts`, `src/components/dashboard/DiscountsClient.tsx`, `src/components/dashboard/discounturi/SertarCod.tsx` (nou), `src/app/(dashboard)/dashboard/discounts/page.tsx`, `src/components/storefront/sections/checkout/checkout-core.ts`, `src/components/ministore/OrderModal.tsx`, `src/lib/actions/abandoned-cart.actions.ts`, `src/app/api/cron/abandoned-recovery/route.ts`, `src/lib/abandoned/capcane-automatizare.ts` | **Cinci stari** in loc de doua („Activ"/„Inactiv"): activ, oprit, programat, expirat, epuizat, cu regula scrisa intr-un SINGUR loc pe care il intreaba si lista, si filtrul, si sertarul. **Doua cifre de utilizari** („26 · 30 in total"), fiindca `uses_count` scade inapoi la anulare. **Sertar lateral** cu fisa codului si comenzile pe care s-a folosit. **Programare** („de cand pana cand"). ⚠⚠ Ziua scrisa de comerciant e acum o ZI ROMANEASCA: pana azi „31 august" insemna 03:00 dimineata in chiar ziua aceea, deci codul murea cu 21 de ore mai devreme. ⚠⚠ Mesajul unic (`ESEC_CUPON`) s-a mutat in `lib/discounts/mesaj.ts` si acopera acum SI plasarea comenzii: `placeOrder` si `placeCartOrder` raspundeau cu un text al lor, care de azi e si neadevarat, si un oracol pe un drum public. Motivul adevarat pleaca in `error_logs`. ⚠ Si vitrina scoate singura cuponul cand chiar el a oprit comanda — altfel omul ramanea blocat, fiindca mesajul unic nu mai are voie sa-i spuna de ce. ⚠⚠ **Trei plase care nu existau deloc**: `validateDiscount` si `claim_discount_use` n-aveau NICIO proba, desi tin regula anti-enumerare si atomicitatea contorului. Probate cu mutanti: 6 din 6 si 5 din 5 prinse. ⚠ Un cod PROGRAMAT putea pleca azi in emailul de cos abandonat (lista se filtra pe loc, doar pe `expires_at`), iar auto-aplicarea din link il pierdea in tacere: acum lista intreaba `sePoateFolosi`, cronul reciteste codul la trimitere si mesajul pleaca FARA reducere, iar capcana din panou s-a redenumit `cod-nefolosibil` si spune adevarul. Depinde de migratiile **30**-**31**. |
| Discounturi (etapa F) | `src/lib/discounts/per-client.ts` (nou, + proba), `src/lib/discounts/revendicarea-e-o-singura-instructiune.test.ts` (rescrisa), `src/lib/actions/order.actions.ts`, `src/lib/actions/discount.actions.ts`, `src/components/dashboard/DiscountsClient.tsx`, `src/components/dashboard/discounturi/SertarCod.tsx`, `src/app/(dashboard)/dashboard/discounts/page.tsx`, `src/types/database.types.ts` | **O data per client** (sau de N ori), cerut de el. ⚠⚠ Verificarea NU e in `validateDiscount`, si asta e o hotarare de securitate: capatul acela e public, iar daca raspunsul ar tine seama de cine e omul, „valid" ar insemna „numarul asta n-a cumparat niciodata de aici". Oracolul n-ar fi in TEXT, ci in bitul valid/nevalid — mesajul unic nu-l poate inchide. Pe o farmacie sau pe un magazin veterinar, asta inseamna „numarul 07xx a cumparat de acolo". Deci limita se judeca abia la trimiterea comenzii, iar atunci cuponul se scoate singur din cos. ⚠⚠ Cheia omului se socoteste IN SQL: in TypeScript sunt DOUA functii `normalizePhone` care dau raspunsuri deosebite. ⚠ Se spune pe ecran si ce NU opreste limita (cumparatorul isi scrie singur telefonul) si ca ea numara doar de acum incolo pe un cod deja folosit. ⚠ Anularea unei comenzi da inapoi si dreptul OMULUI, nu doar pe al campaniei — hotarare luata pe fata: nedat inapoi, un cumparator caruia i-a picat cardul ar fi ramas blocat pe veci cu un mesaj care nu-i poate explica nimic. ⚠ Probele: 4 mutanti pe probele vechi, 10 pe cele noi, toti prinsi. Depinde de migratia **32**. |
| Discounturi (etapa G) | `src/lib/discounts/restrangere.ts` (nou, + proba), `src/components/dashboard/discounturi/AlegePeCeMerge.tsx` (nou), `src/lib/actions/discount.actions.ts`, `src/lib/actions/order.actions.ts`, `src/components/storefront/sections/checkout/checkout-core.ts` si `CheckoutForm.tsx`, `src/components/ministore/OrderModal.tsx`, `src/components/dashboard/DiscountsClient.tsx`, `src/app/(dashboard)/dashboard/discounts/page.tsx` | **Cod care merge doar pe anumite produse sau categorii**, cerut de el. Procentul cade NUMAI pe liniile potrivite; suma fixa se plafoneaza la valoarea lor; transportul gratuit cere macar o linie. ⚠⚠ **Nimic potrivit = REFUZ, nu reducere zero**: ecranul punea transportul pe zero doar din TIPUL cuponului, deci un „valid cu 0 lei" l-ar fi dat gratuit oricum. ⚠⚠ **Potrivirea e imprumutata de la OFERTE**, nu scrisa a doua oara. ⚠⚠ **Re-validarea tacuta asculta acum CONTINUTUL cosului, nu totalul** — gasit de trei adversari independent: cumparatorul scotea produsul potrivit si punea altul la acelasi pret, iar ecranul pastra reducerea. ⚠ Transportul gratuit se deseneaza dintr-un STEAG intors de server, nu din `type`. ⚠ Bannerul cuponului spune acum si „socotită pe X din coș", fiindca „20% reducere" era adevarat despre cupon si fals despre cos. ⚠⚠ Liniile se construiesc ANUME: `cartItems` NU contine produsul principal al formularului, deci un cod restrans chiar la el, folosit de pe pagina lui, n-ar fi gasit nimic. ⚠⚠ **Categoriile NU se primesc de la browser**: cosul nici nu le stie, si daca le-ar sti n-ar trebui crezut. Probat pe catalogul demo: cos de 1.179 lei, 10% restrans la „Decorațiuni" → 28,90 lei pe cele 289 potrivite, nu 117,90 pe tot cosul; iar un cos fara nimic din categorie se REFUZA. Depinde de migratia **33**. |
| Discounturi (etapa H) + tipografia cardurilor | `migrations/2026-09-22-discounturi-prima-comanda.sql`, `src/lib/dashboard/cifra-pe-un-rand.ts` (nou, + proba), `src/components/dashboard/CardStatistica.tsx`, si cele CINCI pagini care il folosesc (`dashboard/page.tsx`, `StatisticiClient`, `CustomersClient`, `AbandonedCartsClient`, `DiscountsClient`) | **„Doar la prima comanda"**, ales de el dintre trei candidati (ceilalti doi raman nefacuti, cu motivul scris). ⚠⚠ Si TREI cereri de design, tot ale lui: **(1)** cardurile de statistici au trecut SUS, deasupra listei — Discounturile erau singura sectiune unde stateau sub tabel. **(2)** Cifrele nu mai trec pe doua randuri: `15.831,80 lei` la 44px cere ~310px, iar un card dintr-o grila de patru are sub 200px. **(3)** ⚠⚠ Prima varianta a reparatiei alegea marimea PE CARD, si el a vazut pe loc ce iesea — „6" la 44px langa „15.831,80 lei" la 22px, deci cutiile nu mai aratau ca un set. Acum marimea e a RANDULUI, data de cea mai lunga cifra, si e legata in toate cele cinci pagini (proba de cablare o cere pe fiecare). ⚠ Unitatea („lei", „%") intra in socoteala latimii: se scrie langa cifra. Depinde de migratia **34**. |
| Discounturi (paginare) + cifrele din carduri | `migrations/2026-09-22-discounturi-z-paginare.sql`, `src/lib/discounts/lista.ts` (nou), `filtre.ts` (subtiat), `starea-e-aceeasi-si-in-baza.test.ts` (nou, inlocuieste `filtrul-intreaba-aceeasi-regula`), `src/app/(dashboard)/dashboard/discounts/page.tsx`, `DiscountsClient`, `SertarCod`, `src/lib/dashboard/cifra-pe-un-rand.ts` | **Lista se pagineaza in baza**, cu filtru, sortare si numaratoare in Postgres; cautarea, filtrul, sortarea si pagina vin din ADRESA. ⚠⚠ `asezate()` si `catePeStare()` STERSE — lasate, ar fi fost o a doua socoteala pe langa cea din baza. ⚠⚠ Si **trei cereri de tipografie**, una dupa alta: cifrele treceau pe doua randuri la sume mari; apoi tipografia iesea amestecata fiindca marimea se alegea PE CARD; apoi erau prea mici, fiindca pragurile veneau dintr-o regula din burta (0,6 din font pe semn) in loc de o **masuratoare** (0,45). ⚠⚠ Doua cauze gasite: pagina Discounturi era pe `max-w-5xl`, singura din panou mai ingusta decat restul — acum `6xl`; si „lei" era scris cu aceeasi litera uriasa ca suma, in loc sa fie dat SEPARAT ca pe Panou si Statistici. Cu `formatPriceValue`, un milion se scrie „1.000.000" — noua semne, nu treisprezece. Rezultat: 1 milion la 44px, 10 milioane la 40, 100 de milioane la 36. ⚠ Marimea e a RANDULUI, nu a cutiei, si e legata in toate cele cinci pagini cu carduri (proba de cablare o cere pe fiecare). ⚠⚠ Capcana tacuta prinsa: clasele TREBUIE sa ramana siruri literale — compuse la rulare, Tailwind nu le-ar genera si fontul ar cadea pe cel implicit, fara nicio eroare. Depinde de migratia **35**. |
| Oferte (ecran + trei defecte) | `migrations/2026-09-22-oferte-lista-si-cifre.sql`, `src/lib/offers/` (`stare.ts`, `filtre.ts`, `lista.ts`, `o-data-pe-vizita.ts`, noi, + 4 fisiere de probe), `src/lib/dashboard/paginare.ts` (nou), `src/lib/zi-romaneasca.ts`, `src/lib/perioade.ts`, `src/lib/discounts/lista.ts`, `src/lib/offers/offer.types.ts`, `use-afisari-oferte.ts`, `src/lib/actions/offer.actions.ts`, `src/components/dashboard/oferte/` (nou: `SertarOferta.tsx`, `tipuri-ui.ts`), `OffersClient.tsx` (rescris), `OfferForm.tsx`, `CardStatistica.tsx`, `src/components/ministore/OrderBump.tsx` si `CartRecommendations.tsx`, `OrderModal.tsx`, `CheckoutForm.tsx`, `DiscountsClient.tsx` | **Linia de design ca la Clienti / Cosuri / Discounturi**: `max-w-6xl`, carduri de statistici sus, `EtichetaStare` cu patru stari adevarate (activa / oprita / programata / expirata), tabel pe desktop si carduri pe telefon, filtre si cautare care apar abia de la patru oferte, sertar lateral cu fisa, si paginare in baza. ⚠⚠ **Si TREI defecte gasite masurand, nu citind.** **(1)** Baliza de afisari era legata DOAR la pagina de produs, desi comentariul ei scria ca „tot asa se numara si pe celelalte doua suprafete": pe productie, `cross_sell` avea 405 afisari / 0 conversii si `order_bump` 0 afisari / 29 de conversii — fiecare tip numarat pe jumatate, si fiecare jumatate aratand ca o masuratoare intreaga. Legata acum in `OrderBump` (amandoua formularele de comanda) si in `CartRecommendations` (sertarul si cele patru pagini de cos). **Probat pe demo, in browser, pe ruta adevarata**: deschiderea formularului a dus bump-urile 912→913 si 377→378, sertarul de cos a dus un cross-sell 0→1, iar redeschiderea formularului NU le-a mai crescut. **(2)** `OfferForm` scria in cod `starts_at: null, ends_at: null`, deci nicio oferta nu se putea programa, iar o perioada pusa de mana in baza era STEARSA la prima salvare din panou (masurat: zero din 13 oferte aveau perioada). Acum se cere din formular si se preface in ZI ROMANEASCA pe server, cu aceeasi functie ca la coduri. **(3)** Tipurile de oferta erau in CINCI liste, dintre care `OFFER_TYPES_IMPLEMENTATE` n-avea niciun cititor. Un singur tabel acum (`DESPRE_TIPUL_OFERTEI`), din care se deriva restul; iconitele stau separat, fiindca `lucide-react` n-are ce cauta intr-un fisier incarcat de server. ⚠ **`listOffers` a fost STEARSA**: zero apelanti dupa paginare, iar fisierul e `"use server"`, deci fiecare export e un capat pe care browserul il poate chema — o usa care aducea lista intreaga, fara plafon. ⚠ `catePagini` era scrisa de DOUA ori, cu corpuri deosebite (`lib/perioade.ts` intorcea `Infinity` pe `pePagina = 0`); amandoua cheama acum `lib/dashboard/paginare.ts`. ⚠ Doua lucruri vazute pe ecran si reparate pe loc: cardul scria „8417" iar tabelul „1.846" pe acelasi ecran, si rata iesea „4.9%" cu punct in loc de virgula. ⚠⚠ Si o reparatie care atinge SI Discounturile: data trecuta se scria rosu doar cand starea era „expirat", deci rosul se stingea in clipa in care comerciantul oprea codul sau oferta — desi data ramanea trecuta. Eticheta spune ce ai de facut INTAI; coloana de data spune un FAPT. Depinde de migratia **36**. |
| Oferte (cele sapte cereri: 1, 2, 7) | `src/lib/offers/porti.ts` (nou, + proba), `offer.types.ts`, `offer-pricing.ts`, `offers.ts`, `src/lib/actions/offer.actions.ts`, `src/lib/actions/order.actions.ts`, `src/components/ministore/OrderModal.tsx`, `src/components/storefront/sections/checkout/checkout-core.ts`, `src/components/dashboard/OfferForm.tsx`, `OffersClient.tsx`, `DiscountsClient.tsx` | **(1)** Eticheta de tip si data nu se mai rup pe doua randuri (si la Discounturi). **(2)** Paginatia exista deja, pusa odata cu redesignul. **(7) PORTILE OFERTEI DE CHECKOUT**, alese de el dintre trei variante: nu un constructor cu SI/SAU, ci patru propozitii — „cosul trece de X lei", „in cos sunt cel putin N bucati", „in cos se afla", „in cos NU se afla". ⚠⚠ **Nu poate strica nimic**: masurat pe productie, ZERO din 13 oferte au `conditions` pus, deci pana nu bifeaza cineva ceva nu se schimba nimic. ⚠⚠ **Stubul EXISTA si nu facea nimic**: `trigger.conditions` era parsat de `parseOfferTrigger` si nu-l citea NIMENI, scris pentru „Faza 3". ⚠⚠ **O singura functie, doua cai**: `poartaTrece` la afisare (`resolveCartOffers`) si `deCeNuTrece` la comanda (`refuzaOferta`), lipite de o proba care cere ca amandoua s-o cheme cu aceiasi trei parametri si ca niciuna sa nu-si scrie propria comparatie pe praguri. ⚠⚠ **Gaura gasita la proiectare, inainte sa fie scrisa**: la afisare produsul oferit NU e in cos, la comanda E deja linie — deci un cos de 150 lei cu un bump de 60 ar fi trecut o poarta de 200, cu chiar produsul pazit. De-aia cosul se tine PE PRODUS si poarta se intreaba pe cosul FARA ce aduce oferta. ⚠ Ce vine din browser hotaraste doar ce se ARATA (a arata nu costa bani); pretul se ia din baza si poarta se pune din nou la plasare, pe preturile CHIAR PLATITE — `products.price` nu e pretul de pe ecran la variante (masurat: 156,80 fata de 438,00). ⚠ `ContextComanda.cos` e OBLIGATORIU, ca `tsc` sa arate fiecare loc care-l construieste: optional, un apelant care il uita ar fi trecut toate portile in tacere. ⚠ Probat cu 19 probe, 2 mutanti prinsi, si **in browser pe ruta adevarata**: poarta la 5.000 lei pe un cos de 2.050 ascunde bump-ul, poarta la 2.000 il aduce inapoi. Nicio migratie. |
| Oferte (cererile 3 si 4) | `src/lib/offers/amplasare.ts` (nou, + proba), `fbt-pricing.ts`, `bump-pricing.ts`, `fbt-in-cos.ts`, `offer.types.ts`, `offer-pricing.ts`, `offers.ts`, + proba noua pentru cantitati; `src/components/ministore/ProductOffers.tsx` si `OrderModal.tsx`, `ProductPageClassic.tsx` si `ProductPageDetailed.tsx`, `src/components/dashboard/OfferForm.tsx`, `scripts/lint-prag.json` | **(3) ASEZAREA SETULUI**: se poate pune langa pret, sub butoane. Implicita ramane banda lata de azi, deci oferta care exista pe productie nu se muta. Niciun camp nou in baza: `display` e deja jsonb. ⚠⚠ Partea grea n-a fost desenul, ci BALIZA: doua bucati de ecran cer doua balize, iar lista care se DESENEAZA si cea care se NUMARA trebuie sa fie aceeasi — le imparte acum o singura functie, cu ACELASI predicat pe care il foloseste si `ProductOffers`. ⚠ Si mutarea SCHIMBA INTELESUL cifrei de afisari (jos = „a derulat pana acolo", sus = aproape „a deschis pagina"); scris pe ecran, sub alegere. ⚠ Gasit de adversar: `TABEL[type].ceva` in parser ar fi ARUNCAT pe un tip nerecunoscut, pe drumul fiecarei pagini de produs — vitrina ar fi iesit alba. **(4) CANTITATI IN SET** („2 becuri + 1 lustra"), camp nou optional in `config`, fara migratie. ⚠⚠ Proba de temelie nu e ca merg cantitatile, ci ca o oferta FARA ele da exact pretul de ieri: cazul LIVE de la BricoSmart da aceleasi patru numere. Si parsarea scrie in jsonb doar ce trece de o bucata, deci un set de cate una produce acelasi rand ca azi. ⚠⚠ Patru socoteli de bani presupuneau „o bucata din fiecare" si s-au mutat DEODATA: pretul setului, impartirea economiei, asezarea pe liniile din cos si aplicarea la comanda. Cotele se fac pe VALOARE, rezultatul ramane UNITAR — pe linie, s-ar fi inmultit a doua oara. ⚠ Venitul se socoteste PE BUCATI: ramas unitar, `orders.offer_discount_amount` si `offers.revenue_added` s-ar fi contrazis pe aceeasi comanda (gasit de adversar). ⚠ Stocul trebuie sa ajunga pentru cate cere setul, altfel butonul ar duce la o comanda refuzata la ultimul pas. ⚠ NU s-a facut ce cerea planul la un moment dat (cantitatea in verificarea `schimbat` din `setulOfertei`): ar fi transformat un filtru de AFISARE intr-o poarta care OPRESTE comanda. ⚠ Probat in browser pe ruta adevarata: cardul scrie 371,20 fata de 387 lei, formularul duce `+ Lumanare 142,20 lei`, total 1.182,19 — la banut. 31 de probe noi, 7 mutanti prinsi. ⚠ Pragul de lint a SCAZUT de la 54 la 53: `window.location.href = …` s-a scris ca `assign(…)`, identic la purtare, dar vazut de analiza ca apel, nu ca scriere peste o variabila din afara componentei. |
| Oferte (cererile 5 si 6) | `migrations/2026-09-23-oferte-stoc-si-cele-mai-vandute.sql`, `src/app/api/cron/oferte-fara-stoc/route.ts` (nou), `vercel.json`, `src/lib/offers/offer.types.ts`, `offers.ts`, `lista.ts`, + proba noua; `src/components/ministore/CartRecommendations.tsx`, `src/components/dashboard/OffersClient.tsx` si `OfferForm.tsx`, `src/app/(dashboard)/dashboard/offers/page.tsx`, `src/types/database.types.ts` | ⚠⚠ **SINGURELE DOUA DIN CELE SAPTE CARE POT SCHIMBA CE VEDE UN CUMPARATOR AZI**, deci implicitele reproduc purtarea de acum si proba de temelie e tot „un rand fara campurile noi se poarta ca ieri". **(5) FARA STOC**: trei stari pe ecran — intreaga / **ciuntita** (inca se vede) / **moarta** (nu se mai arata deloc) — plus a patra, `necunoscut`, care NU e „e bine": ofertele automate n-au lista fixa, iar scrise „intreaga" ar fi fost o liniste neverificata. Eticheta sta LANGA starea ofertei, nu in locul ei: o oferta poate fi „Activa" si totusi fara stoc, chiar cazul care se ascundea. ⚠⚠ Notificarea in clopotel se da DOAR pentru ce a murit de tot, si O SINGURA DATA (`fara_stoc_anuntat_la`), cu DRUM INAPOI cand stocul se intoarce — fara el ar fi functionat exact o data pe oferta, pe viata. Se scrie INTAI urma, apoi notificarea. ⚠ Cele doua suprafete de recomandare se poarta DEOSEBIT azi (pe pagina se arata „Epuizat", in cos se arunca) si asta RAMANE implicita; bifa noua le uniformizeaza si scrie pe ecran ce face. Adversarul a prins ca „ramura de cross_sell" pe care se sprijinea planul nu exista: ternarul deserveste si FBT, si bump. **(6) RECOMANDARI**: trei metode, dintre care `categorie_noi` e CHIAR purtarea veche sub alt nume; metoda se DERIVA din `autoByCategory` cand campul lipseste, si cele doua se scriu impreuna. `categorie_vandute` cade inapoi pe cele mai noi cand magazinul n-a vandut inca. ⚠⚠ `maxProducts` exista de mult si era CABLAT la 4 in formular: optiunea era acolo si nu se putea atinge. Si sertarul de cos taia in tacere la SASE. ⚠ „Exclude ce e deja in cos" NU s-a facut pe pagina de produs, si nu din lipsa de timp: acolo serverul nu stie cosul. In cos merge deja. ⚠ Probat pe ruta adevarata: cronul da 401 fara secret, `anuntate: 1` la prima rulare cu randul in clopotel, `0` la a doua, `uitate: 1` dupa ce stocul se intoarce. 20 de probe noi. Depinde de migratia **37**. |
| Oferte: trei tipuri noi (upgrade, „cumperi X primesti Y", cadou) | `migrations/2026-09-24-oferte-upgrade-bogo-cadou.sql`, `src/lib/offers/linii-acceptate.ts` (nou, + proba), `offer.types.ts`, `offer-pricing.ts`, `offers.ts`, `lista.ts`, `src/components/ministore/OrderBump.tsx` (redenumit `OferteDinFormular`), `OrderModal.tsx`, `src/components/storefront/sections/checkout/checkout-core.ts`, `CheckoutForm.tsx`, `CheckoutSummary.tsx`, `src/components/dashboard/OfferForm.tsx`, `OffersClient.tsx`, `oferte/tipuri-ui.ts` | **Toate trei se bifeaza in FORMULARUL DE COMANDA**, ca bump-ul, si merg pe aceeasi cale probata: produsul intra ca LINIE plus id-ul ofertei, iar serverul re-judeca tot. ⚠⚠ **De ce nu pe pagina de produs**: o oferta nu poate ieftini decat o LINIE, iar produsul din comanda directa NU e linie — pretul lui se socoteste inainte de oferte si `offer_discount_amount` doar consemneaza. ⚠⚠ **Nicio coloana noua**: `offers.type` e `text` fara `check`, iar cele patru campuri noi stau in `config` (jsonb). Migratia 38 rescrie DOAR `offer_stoc`, ca semnul de stoc sa cuprinda si tipurile noi. ⚠⚠ **Proba de temelie: o configuratie veche NU capata niciun camp** — `inlocuieste` se scrie doar cand e OPRIT, exact ca la cantitati. **UPGRADE**: randul arata DIFERENTA („+20 lei"), nu pretul; bifat, produsul mic iese din comanda. ⚠⚠ La schimb, declansatorul NU se mai poate cere la plasare (chiar oferta l-a scos), deci pretul de schimb il poate lua oricine trimite id-ul cu produsul mare in comanda — expunere MARGINITA la o bucata, scrisa pe fata in formular, si se strange cu portile. **BOGO**: se numara BUCATILE declansatorului, fara cele daruite (altfel oferta se hraneste singura). ⚠⚠ Produsul primit trebuie sa fie ALTUL; pentru „2 la pretul de 1" din acelasi produs exista „Reducere cantitate", si e mai buna — scris in formular si refuzat la salvare. **CADOU**: portile sunt chiar oferta; cu „clientul alege", reconstituirea are ramura ei, fiindca regula bump-ului ar fi refuzat in tacere orice cadou in afara de primul. ⚠⚠ **DOUA DEFECTE ADEVARATE, prinse in browser**: (a) cosul trimis pentru porti era pe PRETUL DE CATALOG, nu pe cel incasat — cos de 294,20 raportat ca 302, deci un cadou cu poarta la 300 se arata, se bifa si comanda era OPRITA (checkout mort); `lineUnit` NU e raspunsul, ci `lineTotal / cantitate`. (b) lista de oferte se recerea pe cosul DEJA SCHIMBAT, deci un upgrade bifat isi pierdea declansatorul si DISPAREA — cosul scadea cu 49 si nu crestea cu 69. ⚠ Probat pe ruta adevarata, comanda **#1356**: lumanare mare 69, set suporturi 0, `offer_discount_amount` 55, total 324,19 — exact ce scria pe ecran; amandoua formularele. 25 de probe noi. ⚠ RAMAS: upgrade-ul nu apare cand produsul de schimbat nu e linie de cos; cadoul nu se arata daca pragul se atinge doar cu ce aduc alte oferte bifate (directia sigura). |
| Oferte: auditul final pe toate sabloanele | `src/components/ministore/OrderBump.tsx`, `OrderModal.tsx`, `src/components/storefront/sections/checkout/checkout-core.ts`, `src/lib/cosul-abandonat-poarta-valoarea-reala.test.ts` | Cerut de el: „asigura-te ca se vede bine pe TOATE versiunile de design de cos sau de checkout". ⚠ Ofertele se deseneaza intr-un SINGUR loc (`OferteDinFormular`), randat de `CheckoutForm` — pe care il folosesc amandoua modelele de checkout (`classic` si `page_two_col`) — plus `OrderModal`. Probat din toate cele PATRU modele de cos (`classic`, `page_split`, `page_wide`, `page_compact`): din fiecare, drumul catre formular arata cele trei oferte. ⚠⚠ **LA 390px NUMELE PRODUSULUI RAMANEA CU 46 DE PIXELI** („Lumâna…"): cardul are 253px, din care bifa, miniatura si distantele iau 116. Pretul COBOARA acum sub nume pana la `sm` si sta in dreapta de la `sm` in sus, din ACELASI bloc desenat in doua locuri. Verificat la 320/360/390/430/640/700 si desktop. ⚠⚠ **DOUA COMENTARII SPUNEAU PE DOS**: amandoua sustineau ca `lineUnit` / `pretBucataCos` „are treptele de cantitate". Nu are, si nici nu trebuie — `CartProvider` il defineste dinadins ca „o bucata INAINTE de trepte", ca eticheta „N buc x P" sa se inmulteasca la totalul liniei. Afirmatia m-a trimis pe drum gresit chiar in ziua aia. Indreptate, si **instantaneul cosului abandonat** — care le credea — salveaza acum pretul chiar incasat (`lineTotal / cantitate`) in amandoua formularele; din numerele alea se face „Valoare cosuri abandonate" si pragul „trimite doar peste 300 de lei". ⚠ Plasa care pinuia `price: lineUnit(i)` a CAZUT la o schimbare care facea numarul mai bun — apara forma, nu regula; rescrisa pe sursa numarului. ⚠ Instantaneul ia cosul INTREG, nu pe cel de dupa scoaterea facuta de un upgrade. ⚠⚠ RAMAS DE STIUT: **o linie de 0 lei n-a fost niciodata facturata din platforma** (formularul refuza pana acum pretul fix zero; `docs/facturare/SMARTBILL.md`: din 241 de documente, zero reduceri promotionale). Daca SmartBill o refuza, cade EMITEREA, nu comanda — facturarea e fire-and-forget pe schimbarea de stare. ⚠ Liniile pentru cupon si pentru pixeli folosesc tot `lineUnit`: purtarea de dinainte, neatinsa, alte cai de bani. |
| Pepita: preturi in feedul de stoc | `src/lib/pepita/serializare.ts`, `pret.ts`, `articole.ts`, + `serializare.test.ts`, `feed.test.ts`, `pret.test.ts`; `docs/pepita/README.md` | Cerut de ei prin email, dupa ce VetDepo (`okxi`, vetdepo.ro) le-a dat adresele: „adauge preturi in fluxul de stocuri, deoarece acest parametru este necesar si in noul nostru procesor de fluxuri". ⚠⚠ **Cererea e mai NOUA decat documentul lor de pe disc**: tabelul din `xml-format-2026-09-08.txt` pentru feedul de stoc cere DOAR `<Id>` si `<Availability>`, si exact asta trimiteam. Abaterea e scrisa in cod, la `stocXml`. ⚠⚠ Blocul de preturi e scris ACUM O SINGURA DATA (`preturiXml`) si il folosesc amandoua feedurile: acelasi `<Id>` isi spune pretul o data pe zi si o data pe ora, iar despartite, marketplace-ul ar fi pastrat ultimul citit. Proba pe ruta adevarata: 81 de produse, `<Prices>` IDENTIC in cele doua feeduri, zero deosebiri. ⚠ **Pietrele de mormant NU capata pret**: regula lor spune „un pret 0 nu e acceptat", iar un articol disparut nu mai ARE pret; tabelul lor de stoc nu cere preturi. ⚠ Zero nu se poate intampla la restul: articolele cu pret zero sunt oprite inainte de feed, cu motiv pe ecran. ⚠⚠ **DEFECT GASIT CU OCAZIA ASTA**: `<ShippingPrice>` NU se convertea in moneda pietei — conversia era o inchidere locala in `preturilePentruFeed`, iar transportul nu trecea pe acolo. La cursul de 80,5 al pietei maghiare, produsul pleca la 38.796,98 HUF si transportul la 19,99 HUF (vreo 25 de bani). Expunere ZERO azi (`shipping_price` nescris la toate cele trei magazine cu Pepita), dar `okxi` are SAPTE piete cu cursuri scrise. Conversia e acum o functie singura (`inMonedaPietei`). ⚠ Fara migratie, fara coloane noi. |
| Rebranding (culoare + sigla) | `src/app/stil-comun.css`, `src/components/ui/Logo.tsx`, `scripts/brand/genereaza-sigle.mjs` (nou), `public/` (13 fisiere de sigla), `public/site.webmanifest`, plus 79 de locuri unde verdele era scris in cod | Verdele marcii e `#07c527`, verdele purtator `#008215` (5,00:1, aceeasi nuanta coborata pana trece pragul). Se verifica: panoul, site-ul de prezentare, o vitrina reala, un e-mail trimis, fila din browser si pictograma pe telefon. ⚠ Magazinele existente isi pastreaza culoarea lor, deci o vitrina care arata tot verde vechi NU e un defect. ⚠ Bannerul de share (`og-image.png`) e acum doar sigla pe fundalul marcii: cel vechi avea titlu scris cu fontul marcii si o fotografie, iar fontul vine din Google Fonts la build si nu exista ca fisier in depozit. De cerut designerului daca se vrea inapoi varianta cu titlu. |

---

## E2. Pagini de proba

Niciuna. Cele doua pagini facute ca sa se vada dintr-o privire toate etichetele de stare
(`dashboard/etichete-proba`) si toate alertele de cont (`dashboard/alerte`) au fost sterse dupa
ce si-au facut treaba, la cererea lui. Nu mai e nimic de scos inainte de unire.

⚠ Daca se mai face vreuna, se trece tot aici: nefiind legata din meniu, nu se vede in panou, dar
oricine cu cont ajunge la ea scriind adresa.

## F. Datorii cunoscute, de hotarat inainte de final

- `RAPORT-POPULARE.json`: 39 de defecte de cod si 61 de lipsuri de ecran gasite la popularea
  bazei demo. Nu sunt atinse inca.
- Cotele de TVA din Setari sunt inca 19 / 9 / 5, nu 21 / 11.
- ⚠ **`subtotal` nu inseamna acelasi lucru pe toate canalele.** Masurat pe baza demo, la 26
  din 95 de comenzi `subtotal + transport + ramburs - reduceri` nu da `total`. Comenzile de
  marketplace isi scriu `subtotal` FARA TVA, pe cand `total` e cu TVA (comanda 7110:
  270,25 x 1,21 = 327,00); unele comenzi din magazin poarta in total sume care n-au coloana
  lor. Fila Vanzari nu mai asaza cifrele ca pe o adunare si spune de ce, dar defectul de
  fond ramane in datele de la ingest. De hotarat daca se indreapta la ingest sau se lasa asa
  si se scrie peste tot ca `subtotal` e o fotografie, nu o componenta a totalului.
- ⚠⚠ **`order_source` e NULL la 64 de comenzi**, la 15 magazine, de la 31.05.2026 pana la
  16.09.2026, deci nu e doar istorie veche. Urmarea, masurata pe productie pe 23.09.2026:
  regula scrisa firesc `not (order_source ? 'marketplace')` intoarce NULL pentru ele si le
  ARUNCA in tacere, adica 279 de comenzi in loc de 343. Forma buna e
  `not coalesce(order_source ? 'marketplace', false)`. Panoul de comenzi NU are defectul
  (filtreaza cu `.is("order_source->>marketplace", null)`, care prinde si NULL), dar orice cod
  nou care desparte vitrina de marketplace intra direct in el.
- `orders_daily_revenue` (grupare pe ziua **UTC**) nu mai e chemat din niciun loc de cod:
  verificat cu `git grep`, ramane doar in `migrations/`, in tipuri si in doua comentarii.
  De hotarat daca il stergem din baza sau il lasam. Cat timp exista, e o capcana: urmatorul
  care cauta „venit pe zile" il gaseste primul si primeste zile UTC.
